# T-101 packages/tokens post-fix 통합 판정

- 리뷰 종류: full post-fix, 독립 reviewer 2인
- 기준 commit: `4c33a5d951e32df0c2170b7a865f324d122dbe05`
- 기준 tree: `aa81767746cb40641e43397af19082456aaacb3b`
- 수정 전 candidate: `f8894e293ca9677f457011197052cd55dbdc9696`
- PR 원 base: `cf2c610cecfa7a9b8f7a035c9529d7a296e27527`
- task: [T-101](../../tasks/T-101-tokens-package.md)
- 최종 판정: **A PASS / B PASS**
- 신규 P0–P3 finding: **0건**
- 최종 판정 시각: 2026-09-07 KST

## 반복 no-go의 근본 원인

초기 후보는 코드가 없는 상태가 아니라 acceptance 계약이 서로 다른 세 층으로 분리되어 있었다. 생성기와 CSS에는 같은 값이 여러 번 적혔고, CI는 `build`가 커밋된 생성물을 먼저 덮어쓴 뒤 `check`하여 drift를 숨겼다. 시험은 일부 dark 값과 파일 존재만 확인해 타입·계층·브라우저 상속 실패를 놓쳤다. 따라서 리뷰어가 서로 다른 문제가 아니라, 검증 순서와 실행 의미가 고정되지 않은 동일한 구조적 결함을 반복해서 발견했다.

이번 수정은 다음 경계를 하나의 검사 계약으로 묶었다.

1. `tokens.css`를 유일한 값 정본으로 두고 DTCG 2025.10 자료형·`$root` 계층·alias/profile 참조를 생성기에서 파생한다.
2. `dark-media.css`를 dark map에서 생성하고, CI가 build 전에 `--check`를 실행한 뒤 재생성 후 Git diff를 확인한다.
3. Tailwind v4 `z-kt-*`, v3 `transitionTimingFunction`, OS media dark, class dark 상속, Tailwind 없는 hairline을 실제 CSS/Chromium probe로 고정한다.
4. light/dark 44개 값을 전수 비교하고, 생성물 변조·dark 값 변조가 선행 검사 또는 시험에서 실패하는지 확인한다.

## 최초 finding disposition

초기 원본은 [A evidence](evidence/2026-09-07-t101-post-fix-01-initial-reviewer-a.md)와 [B evidence](evidence/2026-09-07-t101-post-fix-01-initial-reviewer-b.md)에 immutable 상태로 보존했다.

| ID | 원 심각도 | 처분 | 확인한 수정 |
|---|---:|---|---|
| A-T101-P1-01 | P1 | FIXED | DTCG number/dimension/duration/cubicBezier/color/fontFamily/shadow 변환, `$root`, alias/profile 참조와 공식 schema 검증 |
| A-T101-P2-02 | P2 | FIXED | `theme.css`의 다섯 `z-kt-*` `@utility`와 Tailwind v4 생성 probe |
| A-T101-P2-03 | P2 | FIXED | v3 preset 키를 `transitionTimingFunction`으로 변경하고 ease 생성 probe 추가 |
| A-T101-P2-04 | P2 | FIXED | `dark-media.css`의 class override 제거, Tailwind 기본 media variant와 OS dark 계산값 검증 |
| A-T101-P2-05 | P2 | FIXED | `base.scoped.css`의 descendant `color-scheme: light` 제거, root/surface/input dark 상속 검증 |
| A-T101-P2-06 | P2 | FIXED | profile 참조와 dark-media 생성 경로를 정본에 연결하고 정본 변조 회귀 추가 |
| A-T101-P2-07 | P2 | FIXED | workflow build 전 `check`와 생성 후 `git diff --exit-code`를 추가하고 drift 주입 실패 고정 |
| A-T101-P2-08 | P2 | FIXED | light/dark 44개 전수 deep comparison과 dark warning-tint 변조 음성 사례 추가 |
| B-P2-01 | P2 | FIXED | 생성물 7개 주입 시 양 OS에서 build 전 check가 exit 1이고 주입 내용 보존 |
| B-P2-02 | P2 | FIXED | class/media dark 및 scoped native control의 computed `color-scheme` 확인 |
| B-P2-03 | P2 | FIXED | Tailwind 없는 일반 CSS hairline/control-line과 scoped surface containment 확인 |
| B-P3-04 | P3 | FIXED | T-102의 구현·검사·예정 경로를 `packages/tokens/aliases`로 정렬 |

## 독립 reviewer 원본

| reviewer | 실행 ID·판정 | 기준 상태 | 원본·SHA256 |
|---|---|---|---|
| A: 토큰/DTCG·Tailwind·브라우저 | `A-T101-POST-20260907-221508`, PASS | 시작·종료 `4c33a5d`, tree `aa817677`, detached clean | [원본](evidence/2026-09-07-t101-post-fix-01-reviewer-a.md), `D89FBE5EF075ED6F9EEBA20E9CAD4A713631B19A35A21E76D2EFF14F7F231AFB` |
| B: CI·drift·CSS 경계·이관 | `reviewer_b-t101-post-fix-4c33a5d-20260907-221537`, PASS | 시작·종료 `4c33a5d`, tree `aa817677`, detached clean | [원본](evidence/2026-09-07-t101-post-fix-01-reviewer-b.md), `87A3A2B54E34D89870A77676E78C362AC5A32BEF6BB88AA2858C0A21EDC57779` |

초기 BLOCK 원본 hash는 A `F94656D7A2B88954C23E7A7B26FCDE5A4E8E23D504A42FF45D9C67F3BF159E49`, B `A7395DFEB8A601D7DE76EBBE10B4C014B2116B834DD528568F5697355159B527`이다. post-fix 원본은 [evidence 디렉터리](evidence/)에 각각 별도 파일로 보존했으며, 두 reviewer는 상대 결과를 열람하지 않았다. manifest는 [여기](evidence/2026-09-07-t101-post-fix-01-manifest.md)에 둔다.

## 검증 결과

- PR #11 정확한 head `4c33a5d`의 [CI run 34126309766](https://github.com/digitie/kor-travel-common/actions/runs/34126309766)에서 docs, tools Ubuntu, tools Windows, secret-scan, check-versions, packages 6개 job이 모두 성공했다. packages job은 Node 22.23.1/npm 11.19.1, build 전·후 생성물 검사, 7 tests/7 pass, tarball 설치를 확인했다.
- Windows와 WSL에서 Python unittest 203개, npm package test 7개, plan/link/SPDX/secret/redaction/version self-check, `git diff --check`를 실행해 오류·skip·finding 없이 통과했다.
- `npm pack` 설치 후 공개 subpath와 ESM 44개 token value를 확인했다. tarball에는 GPL 고지와 CSS 7종·JSON·`index.d.ts`가 포함됐다.
- 공식 [DTCG 2025.10 형식](https://www.designtokens.org/tr/2025.10/format/)과 [JSON Schema](https://www.designtokens.org/schemas/2025.10/format.json)로 후보 JSON을 직접 검증했고 오류는 0건이었다.
- Windows Tailwind v4/v3 컴파일과 Chromium에서 z/ease/duration, OS media dark, class dark, scoped dark 상속, 순수 CSS hairline을 실제 계산값으로 확인했다.

## 남은 gate와 범위

- `NOT_RUN(T-103 대기)`: `kt_contrast` 및 UX lint.
- `NOT_RUN(소비자 후속 task)`: 소비자 저장소 수정·채택, consumer build/e2e·6폭 시각 diff.
- `NOT_RUN(사용자 범위)`: npm/PyPI 게시, GitHub Release, tag 생성.
- `NOT_RUN(후속 merge gate)`: merge 후 main CI와 `codex/release-*` 실제 push CI. 이 기록은 PR head CI와 바꾸어 쓰지 않으며 merge 뒤 별도로 채운다.

따라서 이 보고서는 초기 no-go의 반복 원인을 수정한 candidate의 두 reviewer PASS를 확정하지만, 위 외부·merge 후 gate가 완료되기 전에는 T-101을 DONE으로 표시하지 않는다.