# 2026-09-07 T005c post-fix 02 적대적 리뷰

- Review ID: T005C-20260907-POST2
- 종류: post-fix-01 finding 반영 후보에 대한 전문 영역이 다른 reviewer A/B의 독립 full 재검토
- Review candidate commit: `ae6d25711ac04a03db7b8182bd7ffae4a1b2d852`
- Parent commit: `8a662a4a981f20d62c8ab98d9311a610bdb30b5c`
- 공통 manifest: [post-fix-02 manifest](evidence/2026-09-07-t005c-post2-manifest.md), SHA256 `45ABD6DEC358012B353F1A7AD71D81C1904A5A5BDF2E7982BAA2E4A83576E101`
- 상태: COMPLETE (post-fix 02); 최종 verdict: **BLOCK**

## 1. 독립성·실행 증거

두 reviewer는 동일 manifest와 immutable detached worktree에서 candidate SHA를 확인하고 서로의 raw 결과와 이전 통합 보고를 열지 않았다. 후보·소비자 파일을 수정하지 않았으며 시작·종료 HEAD와 clean 상태를 확인했다.

| 항목 | Reviewer A | Reviewer B |
|---|---|---|
| 전문 영역 | parser·YAML 경계·구조·workflow 출력 | CI·redaction·repo 정책·Docker·정합성 |
| 원본 evidence | [A 원본](evidence/2026-09-07-t005c-post2-reviewer-a.md), SHA256 `8055C1187D9EF94142A6241A14E7FD372979010E9481B54A60B4D7BB70CAADFB` | [B 원본](evidence/2026-09-07-t005c-post2-reviewer-b.md), SHA256 `E869E8895F6FA356D64B611455E7E3E58D19A6B4771350BE980732CCCA38D2C7` |
| Windows 전체 / focused | 176 / 81, skip 0 | 176 / 81, skip 0 |
| WSL 전체 / focused | 176 / 81, skip 0 | 176 / 81, skip 0 |
| validators | 문서·계획·SPDX·secret·redaction·self-check 통과 | 동일 |
| exact PR CI | run `34094677429`, 5 job success | 동일 |

실제 소비자 workflow 실행·원격 action major 조회·Docker pull/inspect·package build/install/publish·merge 후 main CI는 범위 밖 또는 후속 gate로 NOT_RUN이다. 각 raw report에 명령·OS·시간·재현 범위와 NOT_RUN 사유를 보존했다.

## 2. post-fix-02 disposition

| finding 묶음 | 결과 |
|---|---|
| A-P1-01 / B-P1-01 workflow 출력 redaction | **OPEN** — 접두/인용 assignment, 사설 IPv6, password hash가 네 출력 채널에 남음 |
| B-P1-08 repo 표시값의 정책 식별자 오염 | **OPEN** — 표시용 비공개 repo를 예외 조회에도 사용해 만료 예외가 OK로 바뀌고 `mode_source`가 원문을 노출 |
| A-P1-02 / B-P2-05 plain indicator fail-close | **OPEN** — `]bad`, `}bad`, `,bad`가 정상 workflow로 승인 |
| A-P2-03 / B-P1-03 step·reusable job 구조 | **OPEN** — `run: []`·`run: null`과 reusable job `with: []`가 통과 |
| A-P2-05 plain scalar quote 문맥 | **OPEN** — 공백 뒤 plain scalar의 apostrophe/double quote를 인용 시작으로 오인 |
| A-P2-04 / B-P2-04 Docker 이름 구조 | **OPEN** — `a..b`, `a___b`, `reg.-example` 같은 구분자·registry label이 통과 |

local/workflow symlink containment, Unicode surrogate 입력 오류, 기존 URL·AWS·사설 IPv4 redaction, setup-node 공백 참조, 2칸 block/trailing-free flow 문서화는 이번 재현에서 유지됐다. 열린 P1은 문서 기록만으로 닫지 않고 코드·회귀 시험과 두 reviewer의 재검토가 필요하다.

## 3. 다음 수정 수용 기준

1. `.secret-scan-patterns`·`.prod-redaction-patterns`의 접두/인용 assignment·password hash·사설 IPv6·service endpoint 경계를 workflow 모든 출력 채널에 적용한다.
2. registry 정책 조회에는 원 repo 식별자를 유지하고 Finding·`mode_source`·보고서에는 별도 표시값을 사용한다. 유효/만료 예외 회귀를 함께 둔다.
3. YAML plain indicator를 문자별로 fail-close하고, plain scalar가 이미 시작된 뒤의 quote를 인용 시작으로 처리하지 않는다.
4. step `run`은 비어 있지 않은 문자열이어야 하며 reusable job의 `with`도 map이어야 한다.
5. Docker registry host label과 repository component를 Distribution reference 구분자 규칙에 맞게 검증한다.

post-fix-02 원본은 이 report의 링크 파일에 그대로 보존했다. 다음 후보는 별도 immutable manifest와 독립 A/B 재검토를 거쳐야 한다.
