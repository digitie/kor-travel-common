# 2026-09-07 T005c 최종 적대적 리뷰

- Review ID: T005C-20260907-POST3D
- 종류: post-fix-02 이후 코드 후보에 대한 전문 영역이 다른 reviewer A/B의 독립 full 재검토
- Review candidate commit: `5807e535c16310c41c21f9efce87b2113aa17ee5`
- Parent commit: `0f55acc2211718d91ce056b096046054b037d7e4`
- 공통 manifest: [post-fix-03d manifest](evidence/2026-09-07-t005c-post3d-manifest.md), SHA256 `D1AB00A0F4462AEEB1F99E14C6E3FA2C36A5BDD7FA28AE23C6B60278F3C8C451`
- 상태: COMPLETE; 최종 verdict: **A PASS / B PASS**

## 1. 독립성·실행 증거

두 reviewer는 동일 candidate와 manifest의 detached clean worktree에서 서로의 raw와 이전 통합 보고를 읽지 않고 독립적으로 공격했다. 후보·소비자 파일을 수정하거나 commit/push하지 않았으며 시작·종료 SHA/tree와 clean 상태를 확인했다.

| 항목 | Reviewer A | Reviewer B |
|---|---|---|
| 전문 영역 | parser·YAML 경계·구조·출력·회귀 | redaction·registry 정책·Docker·정합성·CI |
| 원본 evidence | [A 원본](evidence/2026-09-07-t005c-post3d-reviewer-a.md), SHA256 `14CA3BEDC77409BD055330D93A9C6EE8062B928AA71362AE0DE1A056028E8F03` | [B 원본](evidence/2026-09-07-t005c-post3d-reviewer-b.md), SHA256 `45C3968184445DD96C9990CED6764DF7E8C94BF472CB6B237FCF7ADA8F1F05CE` |
| Windows 전체 / focused | 179 / 84, skip 0 | 179 / 84, skip 0 |
| WSL 전체 / focused | 179 / 84, skip 0 | 179 / 84, skip 0 |
| validators | 문서·계획·SPDX·secret·redaction·self-check 통과 | 동일 |
| 직접 CLI | 116회 | 100회 + 합성 registry 5회 |
| exact PR CI | run `34097813843`, 5 job success | 동일 |

## 2. 누적 finding disposition

| finding | 최종 결과 |
|---|---|
| A-P1-01 / B-P1-01 workflow 출력 redaction | **FIXED** — assignment 접두/인용 값·URL credential·사설 IPv4/IPv6·password hash·service endpoint와 repo/root/mode_source가 모든 출력 채널에서 비공개 |
| A-P1-02 / B-P2-05 malformed plain indicator | **FIXED** — `@`·backtick·`,`·`]`·`}`와 잘못된 colon/quote/flow 닫힘을 exit 2로 차단 |
| A-P2-03 / B-P1-03 job·step·with 구조 | **FIXED** — 빈/operationless 구조, `run` list/null/빈 문자열, reusable job·step `with` 비-맵을 개별 검증 |
| A-P2-04 제한 YAML 들여쓰기·flow 계약 | **FIXED** — 2칸 block과 trailing-free 단순 flow 범위를 정본에 명시하고 밖의 표기는 exit 2 |
| A-P2-05 / B-P2-09 plain·flow quote 문맥 | **FIXED** — 공백·comma·colon·bracket 뒤 plain quote와 flow quoted item의 colon/hash 조합을 보존 |
| A-P2-06 setup-node canonical | **FIXED** — 공백이 있는 canonical `actions/setup-node`도 Node 하한을 검사 |
| A-P2-07 Unicode scalar | **FIXED** — surrogate/invalid codepoint를 traceback 없이 exit 2 |
| A-P2-08 / B-P2-07 root containment | **FIXED** — workflow 파일/디렉터리와 local action symlink 탈출을 읽기 전에 exit 2 |
| B-P1-08 repo policy identity | **FIXED** — 표시용 비공개 문자열과 registry exception/enforce 식별자를 분리해 EXEMPT/EXEMPT_EXPIRED 판정을 보존 |
| B-P2-04 Docker name grammar | **FIXED** — Distribution separator·registry host label·port와 단일 repository를 구분하고 잘못된 구조를 `OK`로 만들지 않음 |

## 3. 검증·정본

Windows/WSL에서 전체 179개와 T005c focused 84개를 skip 없이 실행했다. 문서 링크(311/2202, 오류 0), plan(102 task, 오류 0), SPDX(29 파일, 오류 0), secret/redaction(386 파일, 발견 0), registry self-check(exit 0), `git diff --check`를 모두 통과했다. [PR CI 34097813843](https://github.com/digitie/kor-travel-common/actions/runs/34097813843)은 exact candidate의 pull_request run이며 docs·tools(Windows/Ubuntu)·check-versions·secret-scan 5개 job이 모두 성공했다.

정본은 [버전 규약](../../standards/versions.md) §3.8·§8과 [T-005c task](../../tasks/T-005c-workflow-static-report.md)다. 이 도구는 제한 YAML subset의 정적 `uses`·setup-node 선언만 읽으며 원격 실행·action major·Docker 이미지 내용은 판정하지 않는다. 구현은 common 저장소와 테스트만 변경했고 소비자 저장소·registry는 건드리지 않았다.

## 4. NOT_RUN과 한계

- 실제 소비자 workflow/CI/파일 변경·빌드·e2e와 T-403 이관은 실행하지 않았다.
- 원격 action 실행·major 조회, Docker daemon/pull/inspect, 전체 YAML 표준 적합성은 범위 밖이다.
- package build/install/publish, npm/PyPI 게시, release push CI와 merge 후 main CI는 최종 merge gate로 남긴다.
- Windows junction·네트워크 공유·경쟁 조건은 범위 밖이며, 일반 파일·디렉터리 symlink 탈출은 양 OS에서 검증했다.

두 reviewer 모두 신규 P0/P1/P2/P3 finding 없이 PASS했다. 이 판정은 위 제한 범위와 이번 immutable candidate에 한정한다.
