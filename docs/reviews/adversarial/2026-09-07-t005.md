# T-005 버전 검사 독립 적대적 리뷰

- Review ID: `2026-09-07-t005`, full, coordinator `/root`.
- Candidate: `409b95c692c073807906e2c1546f022c59a7499c`; base: `219e44d71b3729bb4dda83bf8c2884b63f167e8d`.
- 범위: T-005의 13파일 delta. 소비자 쓰기·제품 gate·전체 SemVer·후속 Python 파서/CI는 범위 밖이다.
- 시각: 2026-09-07 07:05~07:10 KST. 각 실행 시각과 원본은 아래 evidence에 있다.
- 상태: `POST_FIX_REVIEW`. 최초 verdict: **A BLOCK / B BLOCK**. 수정 후 최종 판정은 새 post-fix report에 기록한다.

## 1. 독립 기준선과 원본

[동일 manifest](evidence/2026-09-07-t005-manifest.md)를 각자의 detached worktree에서 검토했다. 두 원본을 서로 열람하기 전에 확정한 뒤 coordinator가 교차 비교했다. 재정렬 commit `3bec3eb`과 candidate의 트리는 `git diff --exit-code`로 동일함을 확인했으며, 리뷰 기준은 원래 commit을 유지했다.

| Reviewer | 전문 영역 | 실행 ID·원본 | 최초 판정 |
|---|---|---|---|
| A | npm·수치·설치 경로·Windows/WSL | [A 원본](evidence/2026-09-07-t005-reviewer-a.md), T005-A-20260907-070533-409b95c | BLOCK |
| B | 정책·차단·예외·CLI·evidence | [B 원본](evidence/2026-09-07-t005-reviewer-b.md), B-T005-20260907-070540-409b95c | BLOCK |

시작·종료 SHA·clean·명령·한계는 각 원본에 보존했다. A는 Windows/WSL 107 tests와 14개씩 공격 입력, B는 Windows 107 tests와 판정/모드 30조합을 실행했다. A의 소비자 6곳 재현과 geo NOT_RUN, B의 7곳·48입력·306행 완전 재현을 서로 다른 실행 결과로 유지한다.

서식 정정(2026-09-07): A 원본의 마지막 빈 줄 하나만 제거했다. finding·문구·판정은 바꾸지 않았고 원 reviewer에게 알렸다. 정정 전 파일 SHA-256은 `0ada79bd8907c6a359f2cff67494996b2ca3c067220b756db66b1856dca46574`다.

## 2. A findings

| ID | 심각도 | 위치·실패 | 영향·수정 |
|---|---|---|---|
| A-P1-01 | P1 | check_npm_locks가 link를 제외 | 축/차단 링크를 NO_LOCK으로 남김 |
| A-P1-02 | P1 | 전이 URL·branch 선언 미검사 | 전이 선언과 resolved를 분리 검사, 기본 registry tarball 구분 |
| A-P1-03 | P1 | blocked 전용 미해석 버전 누락 | 이름 일치와 수치 비교를 분리하고 NO_LOCK 기록 |
| A-P1-04 | P1 | npm 이름을 Python 방식으로 병합 | ecosystem별 패키지 식별자 비교 |
| A-P1-05 | P1 | 빈 교집합 OK, 부분 `>` 하한 오판정 | 지원 하한 문법 제한·빈 교집합/미지원 NO_ENGINES |
| A-P2-01 | P2 | 정상 build metadata NO_LOCK | npm 지원 문법 검증 후 숫자 추출 |

## 3. B findings

| ID | 심각도 | 위치·실패 | 영향·수정 |
|---|---|---|---|
| B-P1-01 | P1 | 차단 전용 미해석 버전 누락 | A-P1-03과 같은 원인. npm/Python 공통 차단 경계 시험 |
| B-P1-02 | P1 | 실행할 수 없는 다섯 구간 차단 숫자 승인 | 자체 검사와 실행 숫자 파서 문법 일치 |
| B-P2-03 | P2 | 공백뿐인 예외 사유/근거 승인 | strip 후 필수값 확인 |

## 4. 교차 판정

원본 9개 ID를 모두 유지하고 두 차단 버전 finding의 중복만 연결한다. 모두 근거가 확인되어 수용했다. 일반 시험 성공이나 report 재현은 발견된 경계 결함의 기각 근거가 아니다. 후보 자체를 merge하지 않는다.

## 5. 반영·재검증

각 ID는 코드·문서·회귀 시험을 수정한 **FIXED(원 reviewer 재확인 대기)**다. 원본 finding의 당시 OPEN·심각도·재현을 고치지 않았다. 새 시험 8개를 먼저 실행해 39개 버전 시험 중 20개 실패를 확인했고, 수정 후 Windows Python 3.14.3·WSL Python 3.11.15에서 각각 전체 115 tests·skip 0을 확인했다. 원본 보고는 보존하고 [수정 후 evidence](../../evidence/t005/README.md#리뷰-수정-후-대조)로 연결한다. 외부 소비자 build·e2e는 NOT_RUN이다.

## 6. Post-fix 재검토

수정 commit을 같은 두 reviewer에게 전달해 자신의 finding과 전체 delta를 재확인한다. 결과는 새 post-fix report와 원본 두 파일에 기록한다. 이 문서는 최초 review의 조치 이력이다.

## 7. 판정 범위

T-005는 IN_PROGRESS이며 PR #3은 draft다. P1 원 reviewer 재확인 전 merge하지 않는다. npm·PyPI 게시 제외와 common 구현 순서 조정은 사용자 지시를 반영할 후속 계획 task이며, 이 리뷰에서 외부 릴리스 gate를 임의 해제하지 않았다.
