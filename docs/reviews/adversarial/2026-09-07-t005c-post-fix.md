# 2026-09-07 T005c post-fix 01 적대적 리뷰

- Review ID: T005C-20260907-03f2cae
- 종류: 초기 finding 반영 후보에 대한 전문 영역이 다른 reviewer A/B의 독립 full 재검토
- Review candidate commit: `03f2cae9a817da96a0eb28ae6487c721464d202f`
- Parent commit: `3ef3fc4f91ba2b6320ba6197176f54917227c31e`
- 공통 manifest: [post-fix manifest](evidence/2026-09-07-t005c-post-fix-manifest.md), SHA256 `CD1FB434401E1B24DCB2E9D1193719D17F9A757E2BA1F6D3A23774CDC6943920`
- 범위: T005c 초기 BLOCK finding 수정, 제한 YAML parser·workflow 출력·path containment·Docker/remote 구조·manifest 연동·정본 문서
- 범위 밖: 전체 YAML/Actions parser, 원격 실행·action major 조회·Docker pull/inspect, 소비자 저장소·package build/install/publish, `actions.checked` 활성화
- 상태: COMPLETE (post-fix 01); 최종 verdict: **BLOCK**

## 1. 독립성·실행 증거

두 reviewer는 같은 manifest와 immutable detached worktree에서 candidate SHA를 검증하고 서로의 원본·통합 결과를 읽지 않았다. 후보 파일을 수정하지 않았으며 시작·종료 HEAD와 worktree clean을 확인했다.

| 항목 | Reviewer A | Reviewer B |
|---|---|---|
| 전문 영역 | parser·YAML 경계·setup-node·출력 계약 | CI·redaction·root/Docker path safety·정합성 |
| 실행 ID | `T005C-A-PF1-20260907-155259-03f2cae` | `T005C-B-POST-20260907-155255-03f2cae` |
| 원본 evidence | [A 원본](evidence/2026-09-07-t005c-post-fix-reviewer-a.md), SHA256 `58A49BB8F12B1FA2266EC79D2B601A0767D0DEFCA24F8F0E9400D43CB042FA32` | [B 원본](evidence/2026-09-07-t005c-post-fix-reviewer-b.md), SHA256 `6DE8520FD13B8E32B3DD080E6CF026F3FD366F38B2BE6967AE0E293B9AA8176D` |
| Windows 전체 / focused | 173 / 78, skip 0 | 173 / 78, skip 0 |
| WSL 전체 / focused | 173 / 78, skip 0 | 173 / 78, skip 0 |
| validators | 문서·계획·SPDX·secret·redaction·self-check 통과 | 동일 |
| exact PR CI | run `34092707302`, 5 job success | 동일 |

두 reviewer의 실제 소비자 workflow 실행·원격 action major 조회·Docker image inspect·게시·merge 후 main CI는 NOT_RUN(범위 밖 또는 후속 gate)이다. 각 raw report에 실제 명령·시간·OS·NOT_RUN을 그대로 보존했다.

## 2. 초기 finding disposition

| finding 묶음 | post-fix 결과 |
|---|---|
| A-P1-01 / B-P1-01 출력 redaction | **OPEN** — AWS/URL marker 일부는 가렸으나 비밀번호 assignment·사설 주소·`--repo` 생략 시 root basename이 stdout/JSON/Markdown/summary로 전파 |
| A-P1-02 YAML malformed fail-close | **FIXED** — colon-space·잉여 bracket·bare alias·잘못된 quote가 exit 2 |
| A-P2-03 / B-P1-03 빈 범위 성공 | **OPEN** — 단독 빈 scope는 닫혔으나 정상 uses와 섞인 빈 job/step·일반 action의 잘못된 `with`가 통과 |
| A-P2-04 block/list indent | **OPEN** — with-first는 수정됐으나 4칸 block·indentless collection·trailing flow separator 계약이 문서와 불일치 |
| A-P2-05 scalar quote/escape | **OPEN** — balanced shell quote·YAML escape는 수정됐으나 plain apostrophe/double quote를 오인해 정상 scalar를 거부 |
| A-P2-06 setup-node canonical | **FIXED** — 공백이 있는 setup-node도 Node floor를 검사 |
| B-P1-02 workflow symlink containment | **FIXED** — workflow 파일·디렉터리 외부 symlink를 읽기 전에 exit 2 |
| B-P2-04 Docker/remote target | **OPEN** — 최초 이동 경로·빈 이름·중복 `@`는 수정됐으나 대문자·중간 port·끝점 repository가 OK |
| A-P2-07 / B-P2-06 Unicode escape | **OPEN** — lone surrogate가 UnicodeEncodeError traceback을 생성 |
| A-P2-08 / B-P2-07 local symlink exit | **OPEN** — root 밖 local symlink가 NO_LOCK으로 남아 report/warn에서 exit 0 |
| B-P2-05 plain scalar indicator | **OPEN** — `@bad`·backtick 시작 plain scalar가 정상 성공 |

중복 A/B 묶음을 합치면 **고유 OPEN 8개**(P1 1개·P2 7개), FIXED 3개다. 모든 OPEN finding은 다음 후보에서 코드·시험·정본 disposition과 두 reviewer 재검토가 필요하다. P1은 risk acceptance나 문서 기록만으로 닫지 않는다.

## 3. 다음 수정 수용 기준

1. `.secret-scan-patterns`·`.prod-redaction-patterns`와 같은 assignment·URL credential·private/internal host 탐지를 workflow의 `declared`·`installed`·`scope`·`roots`·repo 표시에 적용하고 네 출력 채널에서 회귀한다.
2. 각 job의 `steps`, 각 step의 `uses`/`run`/`with` 구조를 개별 검증해 정상 sibling 뒤의 빈 step/job을 건너뛰지 않는다.
3. 2칸 증가 block map/list, trailing separator 없는 단순 flow sequence라는 제한을 정본에 명시하거나 구현을 넓히고, plain scalar indicator와 quote 시작 문맥을 fail-close로 시험한다.
4. Unicode scalar 범위를 벗어난 escape와 local action root 탈출은 traceback/NO_LOCK 대신 일반 입력 오류 exit 2로 닫는다.
5. Docker 첫 registry host/port와 repository segment를 분리해 소문자·끝 separator·중간 port를 검증하고, 잘못된 구조를 OK로 만들지 않는다.

## 4. NOT_RUN

- 소비자 저장소의 원천 대조·수정·빌드·e2e·CI 및 T-403 이관
- 실제 GitHub Actions 실행, 원격 action tag/major 조회, Docker daemon/pull/inspect
- npm/PyPI build/install/publish, tag/Release 생성
- post-fix 02 reviewer 재검토, merge 후 main/release CI

초기·post-fix raw evidence는 이 report의 링크 파일을 그대로 보존한다. 새 수정 후보는 별도 immutable manifest와 독립 A/B 재검토를 시작해야 한다.
