# T-011 post-fix-06 통합 리뷰

- 기준 candidate: `56d6ae11d5917eb2b2e69f8f26f90b903bacceb6`
- parent/base: `3977c00a5b4ffa21a8d29f96dd9531efbe3a0cb5`
- PR: [#10](https://github.com/digitie/kor-travel-common/pull/10)
- 독립 원본: [reviewer A](evidence/2026-09-07-t011-post-fix-06-reviewer-a.md), [reviewer B](evidence/2026-09-07-t011-post-fix-06-reviewer-b.md)
- 원본 SHA256: A `D26C4D2FD285DEB643FBDB9EF62E81C2733FA1F8C03B1432A7184D77CD7980AD`, B `5752E1C7555EE4A24F82AACDE2221F4855F58A55B4731ECD9EB730DB11E03B9E`

## 판정

두 reviewer는 app 중간 symlink 조기 반환과 standalone validator registry self-symlink를 양 OS에서 수정된 것으로 확인했다. 추가로 `check_versions`가 누락 registry 경로 또는 잘못된 schema 값을 포함한 `Registry.load` 예외를 annotation에 그대로 출력하는 공통 redaction 결함을 발견했다. 이 결함은 base에도 존재했지만 T-011의 registry 입력·출력 경계에 포함되므로 수정 전에는 **A BLOCK / B BLOCK**이다.

| 통합 ID | 원본 ID | 심각도 | finding | disposition |
|---|---|---:|---|---|
| T011-R20 | A-P2-15, B-P1-12 | P1 | 누락/self/잘못된 registry 입력의 파일 경로·schema 값이 `check_versions` 오류 annotation에 노출됨 | OPEN → `4680bac`에서 수정 |

기존 T011-R18(app 중간 symlink)과 T011-R19(validator registry self-symlink)는 이 candidate에서 FIXED로 재확인됐다. B의 P1을 통합 심각도로 적용해 registry 원문 노출을 merge 차단으로 유지했다.

## 검증과 범위

candidate에서 Windows/WSL 전체 202 tests·focused 23 tests, 문서·계획·SPDX·비밀·redaction gate와 PR CI 5개 job이 확인됐다. registry 오류 반례는 두 OS에서 재현됐고, 소비자 저장소 build/e2e·공용 패키지 build/install·release asset·npm/PyPI 게시와 실제 채택은 `NOT_RUN(이번 범위 밖)`이다.

`Registry.load` 초기화 실패에서 예외 원문을 버리고 고정된 `레지스트리 입력 구조 오류`만 출력하며 비밀형 경로·schema 값 회귀를 추가한 `4680bacdf285f2cc86f1a18cc1de29ff4129f2a8`에서 수정했다. 이 candidate의 두 reviewer 독립 재검토가 끝나기 전에는 merge gate를 열지 않는다.
