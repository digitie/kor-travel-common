# T-104 · T-020 · T-021 사후 4차 독립 적대적 리뷰 A 원본

- 실행 ID: `A-T104-T020-T021-POSTFIX04-20260908-075305`.
- 시작: 2026-09-08 07:53:05.722 KST. 최종 SHA·clean 관찰 종료: 2026-09-08 07:56:13.230 KST.
- immutable 후보: `2ca6b0c491ddcf542d0c77540b78d18ddf100dfb`, tree `d07d9fc6a6506d1166a73d556b6eae75710d570b`.
- 직전 후보: `f9e8b0494006aebc6b3fa77cdb60c502f50d7182`. 누적 수정 비교 시작: `7ffe525f8a6cfaef4ca4cbab7017306b1994f24a`. 최초 전체 base: `1eb8a78b0fe8ec890b5d15b9f4abfd3f81ef9d59`.
- 격리: `F:/dev/kor-travel-common-wt/review-t104-t020-t021-post2-a` detached. 직전 검증 종료·clean 확인 후 새 후보로 전환. 시작/종료 `git status --porcelain=v1` 모두 빈 출력, SHA·tree 동일. source `.git/config` 변경, 후보/소비자 수동 수정, commit/push 없음.
- 입력: [초기 공통 manifest](2026-09-08-t104-t020-t021-manifest.md), 자신의 [최초 원본](2026-09-08-t104-t020-t021-reviewer-a.md)·[사후 원본](2026-09-08-t104-t020-t021-reviewer-a-post-fix.md), coordinator의 명시적 새 후보 요청. 초기 manifest의 SHA와 이번 SHA를 구별했으며 상대 reviewer 원본·결과를 읽거나 요청하지 않았다.
- 최종 요청 원문: “최종 후보 2ca6b0c에서 원본 파일을 반드시 생성해 주세요: docs/reviews/adversarial/evidence/2026-09-08-t104-t020-t021-reviewer-a-post-fix-04.md. f9e8b04 이후 3줄 문서 delta(조사 ±0.03은 판정 미적용, 반올림 합격 금지)를 포함해 exact SHA/tree, 전체 누적 finding, PASS/BLOCK, SHA256을 기록하세요.”

## 최종 판정

**PASS**. 누적 A finding 7건 모두 **FIXED**, 잔여·신규 P0/P1/P2/P3는 0건이다. 이번 원본이 coordinator가 지정한 최종 후보의 확정본이다. 이전 post-fix-02/03 후보 검토는 후보 교체 전 기록이며 최신 후보 판정을 대신하지 않는다.

## 누적 finding disposition

| 원 ID · 심각도 | 판정 | 확인 근거 |
|---|---|---|
| A-P1-01 · P1 | FIXED | 일반 text/tertiary·status 텍스트 4.5:1과 non-text 3:1 분리. TK-8·T-103·style-delivery·T-104 수용 기준 정렬. muted를 읽기 배경으로 사용하면 해당 4.5:1 쌍 추가. 이번 delta에서 조사 ±0.03과 검사 판정을 명시적으로 분리하고 반올림 합격 금지 |
| A-P2-02 · P2 | FIXED | 타입 스케일의 frontend-stack:127·responsive-web:75·ux-guide:131 참조가 실제 admin 7단/비inline @theme 설명의 TK-5로 연결. 나머지 대비·다크·폰트 TK 참조도 해당 규칙과 일치 |
| A-P2-03 · P2 | FIXED | class `:root:not(.dark)`/media light 전용 override 조건 명시. Chromium에서 light 앱 brand, dark common brand fallback을 확인 |
| A-P2-04 · P2 | FIXED | TK-3 z/shadow/rail 표의 `프로필·등록 예외만`과 TK-6 허용 조건 일치 |
| A-P2-05 · P2 | FIXED | `[data-kt-surface]`에서 semantic과 shadcn alias가 함께 재계산. light/dark 자식의 semantic=1rem, alias=1rem, 실제 radius=16px, primary color=red 확인 |
| A-P1-06 · P1 | FIXED | 두 소비자 license-only 해제 조건·T-021 소비자별 external evidence 행을 후속 코드 PR과 분리. B4·common 코드/provenance·build/quality는 후속 task 책임. 선행 license evidence를 후속 T-454/T-473 코드 PR 없이 생성 가능. 외부 evidence는 OPEN/NOT_RUN 유지 |
| A-P2-07 · P2 | FIXED | common root/dark/profile chart 선언 제거로 앱 root 팔레트 상속 보존. 제거에 따른 breaking은 CHANGELOG Breaking/Migration, adoption §4.4, T-104, 공개 package README, architecture/packages에 기록. 앱 팔레트 선언·전후 diff·시각 evidence를 요구 |

## 이번 3파일 delta와 대비 반례

- `git diff f9e8b04 HEAD`의 제품/규범 변경은 design-tokens, T-103, T-104의 세 문서뿐이다. TK-8은 ±0.03을 조사 수치 재현에만 허용하고 합격 임계값에는 적용하지 않으며, T-103 구현 범위와 T-104 수용 기준도 반올림 합격 금지를 명시한다.
- 실제 stdin Python으로 세 문서의 `±0.03`·`반올림으로` 문구 존재를 확인하고 경계를 계산했다. `(4.47,4.5)`, `(4.49999,4.5)`, `(2.97,3)`, `(2.99999,3)`의 `measured >= required`는 모두 False, `(4.5,4.5)`, `(3,3)`은 True다. 이는 새 문서 수용 기준의 반례 대조이며 아직 없는 T-103 CLI 결과가 아니다.
- 누적 직접 반례 `1.05 / (((136/255+0.055)/1.055)**2.4+0.05)`는 **3.5448862152994**. `#888`/white 일반 크기 캡션·상태 텍스트는 최종 4.5:1 기준에서 미달이며 non-text 3:1과 구별된다.
- 직전 f9e8b04에서 실제 CSS 값을 읽어 var를 풀고 OKLCH→선형 sRGB→상대 휘도를 보조 계산했다. text 12쌍, icon/control-line 8쌍, focus/brand 3쌍, status 4쌍으로 **light 27쌍·dark 27쌍 미달 0**. 추가 tertiary/muted 읽기 쌍은 light **4.0445977917**, dark **4.5705935592**다. 최종 문서는 light muted 읽기 사용 시 쌍 추가·미달/baseline 처리 책임을 명시한다. 기본 쌍 통과를 모든 소비자 화면 통과로 표시하지 않는다.

## 이번 exact 후보의 실행 검증

| 명령·환경 | 실제 결과 |
|---|---|
| Windows `git rev-parse HEAD 'HEAD^{tree}'`, `git status --porcelain=v1` | 시작/종료 exact SHA·tree 동일, clean |
| `git diff --check 7ffe525 HEAD` | exit 0 |
| `python -B -X utf8 tools/validate_document_links.py` | 387 documents, 2375 local targets, errors=0 |
| `python -B -X utf8 tools/validate_plan.py` | 상세 task 106, 오류 0 |
| `python -B -X utf8 tools/check_spdx.py --root .` | 45개 파일, 오류 0 |
| `python -B -X utf8 tools/scan_secrets.py --root .` | 502개 파일, 발견 0, 예외 0 |
| `python -B -X utf8 tools/check_prod_redaction.py --root .` | 502개 파일, 발견 0, 예외 0 |
| `python -B -X utf8 tools/check_aliases.py` | CSS 1개, 오류 0 |
| `npm run check --workspace packages/tokens` | 생성물 clean |
| WSL `uv run --no-project --python 3.11 python -B -X utf8 <위 각 도구와 인자>` 및 diff check | 위와 같은 건수, 모두 exit 0 |

WSL에서는 Windows 생성 worktree의 Git 경로를 Linux에서 해석하도록 해당 프로세스에만 `GIT_DIR=/mnt/f/dev/kor-travel-common/.git/worktrees/review-t104-t020-t021-post2-a`, `GIT_WORK_TREE=/mnt/f/dev/kor-travel-common-wt/review-t104-t020-t021-post2-a`를 설정했다. `.git/config`는 수정하지 않았다.

원격은 `gh run list --commit 2ca6b0c491ddcf542d0c77540b78d18ddf100dfb --json databaseId,headSha,conclusion,status,url`와 `gh run view 34168063255 --json headSha,conclusion,jobs`로 직접 조회했다. [exact CI run 34168063255](https://github.com/digitie/kor-travel-common/actions/runs/34168063255)는 head SHA 일치, docs/packages/tools Windows/tools Ubuntu/secret-scan/check-versions **6개 job 모두 success**다.

## 코드 동일성에 근거한 재사용

`git diff --name-only f9e8b04 HEAD -- packages tools tests .github versions.json`는 빈 출력이다. 누적 `d91ac02..HEAD`의 동일 경로 변경은 package README뿐이며 실행 코드·시험·토큰 값은 같다. 다음은 이번 연속 독립 검토에서 이미 완료한 결과이고 새 SHA에서 재실행했다고 집계하지 않는다.

- Windows d91ac02: Python 3.14.3 `python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` → **238 tests, 67.615s, OK, skip 0**.
- WSL d91ac02: uv Python 3.11.15 `uv run --no-project --with jsonschema --python 3.11 python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` → **238 발견, 41.818s, OK, skip 1**(Windows 8.3 전용), 실제 성공 237건.
- Windows d91ac02·WSL 8427fad: 외부 reviewer helper `.git/codex-audit/t102-a-package.py`를 Python으로 실행해 임시 복사의 npm ci → check → build → check → test → pack → 임시 install/export resolve 완료. 각각 **7 tests/skip 0, pack 19 files, alias 포함/examples 제외, install/export resolve PASS**. Windows Node 25.9.0/npm 11.12.1에는 요구 엔진 `^22.12.0` 경고가 있었으므로 Node 22 로컬 검증으로 표시하지 않는다.
- 값 대조: tokens.css root 44개/dark 44개/TK-3 표 44개 집합 동일, light/dark 88개 값 불일치 0.
- Windows `node -`/Playwright Chromium `setContent`·`getComputedStyle`: class/media light에서 앱 brand `oklch(47% 0.14 255)`, dark에서 common brand `oklch(76% 0.085 169)`; scoped radius/brand는 두 모드 모두 1rem/16px/red. 앱 root chart=purple은 profile 자식에 purple로 상속. 앱 chart 미선언은 빈 변수/투명 배경이며 이관 문서의 breaking과 일치. 최종 후보와 해당 CSS 동일.

## NOT_RUN과 경계

- **NOT_RUN**: 실제 T-103 CLI, 소비자 LICENSE/metadata·license-only PR·B4 원천 대조·build/e2e/6폭 시각 비교, npm/PyPI 게시. common 리뷰가 외부 G-LIC를 닫거나 소비자 채택을 완료하지 않는다.
- Windows Node 22 로컬 pack, Linux 브라우저 CSS 재현은 NOT_RUN. exact CI·WSL 패키지 시험·Windows Chromium 결과를 구별한다.
- 후보·소비자 수동 수정, commit/push, 타 reviewer 원본 열람을 하지 않았다. 패키지 시험의 생성기 호출 후에도 Git 내용 변경은 없었다. 자신의 이전 후보 기록을 덮어쓰지 않고 이 새 원본을 생성했다.

최종 **PASS**: 누적 A finding 7건 FIXED, 신규 P0–P3 0건.
