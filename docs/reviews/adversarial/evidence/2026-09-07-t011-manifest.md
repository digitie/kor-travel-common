# T-011 strict consumer-manifest review manifest

- Candidate commit: `7f715b0efb7c422f0b6212eea612f0c95dcf6fff`
- Parent/base: `3977c00a5b4ffa21a8d29f96dd9531efbe3a0cb5`
- PR: [#10](https://github.com/digitie/kor-travel-common/pull/10), draft
- Task: [T-011](../../../tasks/T-011-consumer-manifest-schema.md)
- 범위: `consumer-manifest.v1` JSON Schema·stdlib validator·10개 초기 manifest·`check_versions`의 저장소 루트 계약·관련 task/runbook/standards 문서와 회귀 시험
- 범위 밖: `packages/tokens`·`packages/ui`·Python 공용 패키지 실물, 소비자 저장소 수정·빌드·e2e, 릴리스 tag/asset, npm/PyPI 게시

## 동일 기준선 확인

```bash
git cat-file -e 7f715b0efb7c422f0b6212eea612f0c95dcf6fff^{commit}
git rev-parse 7f715b0efb7c422f0b6212eea612f0c95dcf6fff^{commit}
git diff --find-renames 3977c00a5b4ffa21a8d29f96dd9531efbe3a0cb5 7f715b0efb7c422f0b6212eea612f0c95dcf6fff
git status --porcelain=v1
```

리뷰어는 source checkout을 수정하지 않고 detached worktree 또는 commit object-only 방식으로 위 candidate를 읽는다. 시작·종료 SHA와 clean 상태를 원본 evidence에 남긴다.

## 수용 기준

1. D-19 필드 집합을 strict 검사하고 `enforce`·미지 필드·잘못된 repo/kind/date/path를 거부한다.
2. 10개 초기 manifest가 validator와 draft 2020-12 schema를 통과한다.
3. `check_versions.py <consumer-root> --manifest <consumer-root>/<app>/kor-travel-common.lock.json`이 lockfiles와 consumer root workflow를 읽고, root 밖 path·symlink·누락을 성공으로 숨기지 않는다.
4. 기존 `check_versions` 최소 fixture와 전체 tools 회귀가 유지된다.
5. 실행하지 못한 consumer/package/release gate는 `NOT_RUN`으로 기록되며 성공으로 표현되지 않는다.

## 검증 명령

```bash
python -B -X utf8 -m unittest discover -s tests -v
python -B -X utf8 tools/validate_plan.py
python -B -X utf8 tools/validate_document_links.py
python -B -X utf8 tools/check_spdx.py
python -B -X utf8 tools/scan_secrets.py --all
python -B -X utf8 tools/check_prod_redaction.py --all
git diff --check
```

Reviewer A는 소비자 계약·schema/validator 경계·fixture coverage를, Reviewer B는 backend/tool 정합성·경로 보안·문서 정본·CI/라이선스 evidence를 서로 독립적으로 공격한다. 원본 결과가 확정되기 전 상대 reviewer 결과를 공유하지 않는다.
