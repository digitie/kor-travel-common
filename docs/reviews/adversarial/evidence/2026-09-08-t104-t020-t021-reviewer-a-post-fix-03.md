# T-104 · T-020 · T-021 사후 3차 독립 적대적 리뷰 A 원본

- 실행 ID: `A-T104-T020-T021-POSTFIX03-20260908-074908`.
- 시작: 2026-09-08 07:49:08.301 KST. 종료: 2026-09-08 07:50:59.817 KST.
- immutable 후보: `f9e8b0494006aebc6b3fa77cdb60c502f50d7182`, tree `e410ff8170ed2ba0bcb380970441d75e58f52a94`.
- 직전 검토 후보: `8427fadaaccce5e9b0ea7caddd567bc174ab45d6`. 누적 수정 비교: `7ffe525f8a6cfaef4ca4cbab7017306b1994f24a..f9e8b0494006aebc6b3fa77cdb60c502f50d7182`. 최초 전체 base: `1eb8a78b0fe8ec890b5d15b9f4abfd3f81ef9d59`.
- 격리: `F:/dev/kor-travel-common-wt/review-t104-t020-t021-post2-a` detached를 직전 검증 종료·clean 확인 후 새 후보로 전환. 시작/종료 `git status --porcelain=v1` 빈 출력, 시작/종료 SHA·tree 동일. 후보·소비자에 수동 변경, commit/push, source `.git/config` 변경 없음.
- 입력: [공통 manifest](2026-09-08-t104-t020-t021-manifest.md), 자신의 [최초 원본](2026-09-08-t104-t020-t021-reviewer-a.md)·[사후 원본](2026-09-08-t104-t020-t021-reviewer-a-post-fix.md), coordinator의 명시적 후보 교체 요청. manifest의 초기 SHA와 이번 요청의 새 SHA를 구별했다. 상대 reviewer 원본·결과를 읽거나 요청하지 않았다.
- 요청 원문: “추가 WCAG P1을 반영해 최종 후보가 f9e8b04로 supersede되었습니다: 일반 text/상태 text 4.5:1, non-text 3:1, surface-muted는 장식 기본·읽기 사용 시 쌍 추가. T-103/TK-8/style-delivery/CHANGELOG/T104 acceptance를 정합화했습니다. 8427fad 검토를 확정하지 말고 f9e8b04 exact SHA/tree에서 전체 delta와 해당 P1을 독립 재검토하고 `...reviewer-a-post-fix-03.md` 원본을 작성하세요. 상대 보고서 읽지 말고 커밋/푸시하지 마세요.”

## 최종 판정

**PASS**. 누적 A finding 7건 모두 **FIXED**. 잔여·신규 P0/P1/P2/P3는 0건이다. 이 판정은 common 문서·공개 CSS 계약에 한정하며 T-103 구현·외부 license evidence·소비자 적용을 완료 처리하지 않는다.

## 누적 finding 재판정

| 원 ID · 심각도 | disposition | 재현·수용 근거 |
|---|---|---|
| A-P1-01 · P1 | FIXED | TK-8 일반 본문/보조/강조/tertiary 텍스트와 status 텍스트를 4.5:1, 비텍스트를 3:1로 분리. T-103:21, style-delivery:98, T-104 수용 기준, CHANGELOG가 같은 계약을 참조. `#888`/white의 3.544886은 이제 일반 텍스트에서 미달. muted를 읽기 배경으로 쓰면 해당 text 쌍을 추가하고 4.5:1 적용 |
| A-P2-02 · P2 | FIXED | frontend-stack:127, responsive-web:75, ux-guide:131의 타입 스케일 참조가 실제 7단 표·비inline `@theme` 설명의 TK-5를 가리킴. 대비·다크·폰트 등 나머지 수정 TK 번호도 해당 절과 일치 |
| A-P2-03 · P2 | FIXED | TK-7의 class `:root:not(.dark)`/media light 전용 오버라이드 조건을 실제 CSS cascade로 확인. light 앱 brand, dark common brand가 각각 적용 |
| A-P2-04 · P2 | FIXED | TK-3 z/shadow/rail 표의 `프로필·등록 예외만`과 TK-6의 조건 일치 |
| A-P2-05 · P2 | FIXED | `[data-kt-surface]` 파생 alias 재계산으로 scoped semantic radius/brand가 자식 shadcn `--radius`/`--primary`에 반영. light/dark 모두 semantic=1rem, alias=1rem, 실제 radius=16px, color=red |
| A-P1-06 · P1 | FIXED | concierge/docker-manager license-only 해제 조건·T-021 소비자별 evidence 행을 후속 코드 채택 task와 분리. B4·common 코드/provenance·build/quality는 후속 T-454/T-472/T-473 등에 남기고, license-only evidence 자체는 먼저 만들 수 있음. 외부 행은 OPEN/NOT_RUN 유지 |
| A-P2-07 · P2 | FIXED | root/dark/profile의 chart 선언을 모두 제거해 앱 root 팔레트가 profile 자식에 보존됨. 기본 chart 값이 사라지는 breaking을 CHANGELOG Breaking/Migration, adoption §4.4, T-104 수용 기준, package README, architecture/packages에 명시하고 소비자 팔레트 선언·전후 diff·시각 evidence를 요구 |

## 새로운 대비 반례와 기본값 확인

- 실제 stdin Python 계산 `1.05 / (((136/255+0.055)/1.055)**2.4+0.05)` 결과는 **3.5448862152994**. 최종 문서에서 추출한 일반 텍스트 임계 4.5 비교는 `False`, 비텍스트 임계 3 비교는 `True`다. 일반 크기 캡션·상태 텍스트를 3:1로 허용하던 반례가 수용 기준에서 닫혔다. 이는 T-103 CLI 실행 결과가 아니다.
- `tokens.css`를 읽는 임시 stdin Python에서 var 참조를 풀고 OKLCH→선형 sRGB→상대 휘도→대비를 보조 계산했다. TK-8의 텍스트 12쌍, icon/control-line 8쌍, focus/brand-foreground/brand-mark 3쌍, status 4쌍을 모드별로 검사해 **light 27쌍·dark 27쌍 미달 0**을 얻었다. 코드/토큰을 변경하지 않았다.
- 추가 읽기 쌍 `text-tertiary`/`surface-muted`는 light **4.0445977917**, dark **4.5705935592**다. 따라서 light muted를 실제 텍스트 배경으로 쓰는 앱은 이 쌍을 추가해 미달/baseline·until을 처리해야 한다. 최종 TK-8·T-103·T-104는 이 조건을 명시하며 기본 읽기 표면 통과를 모든 소비자 화면 통과로 주장하지 않는다.
- T-103의 도구 입력·실제 쌍 추가 처리와 미달 fixture 시험은 아직 후속 task의 구현 수용 기준이다. 이번 결과는 문서와 독립 보조 계산의 대조이며 제품 검사기 PASS로 집계하지 않는다.

## 이번 후보에서 직접 실행한 gate

- `git rev-parse HEAD 'HEAD^{tree}'`, `git status --porcelain=v1`, `git diff 8427fad HEAD -- <5개 변경 문서>`, `git diff --check 7ffe525 HEAD`: exact SHA/tree·clean, 새 delta 5파일 전체 확인, 공백 오류 없음.
- Windows `python -B -X utf8 tools/validate_document_links.py`: **387 documents, 2375 local targets, errors=0**.
- `python -B -X utf8 tools/validate_plan.py`: **상세 task 106, 오류 0**.
- `python -B -X utf8 tools/check_spdx.py --root .`: **45개, 오류 0**.
- `python -B -X utf8 tools/scan_secrets.py --root .`, `tools/check_prod_redaction.py --root .`: 각각 **502개 파일, 발견 0, 예외 0**.
- `python -B -X utf8 tools/check_aliases.py`: **CSS 1개, 오류 0**. `npm run check --workspace packages/tokens`: **생성물 clean**.
- WSL에서도 `uv run --no-project --python 3.11 python -B -X utf8 <위 각 도구와 인자>` 및 `git diff --check 7ffe525 HEAD`를 실행해 동일 건수·exit 0을 확인했다. Windows worktree의 Git 경로를 Linux에서 읽기 위해 프로세스에만 `GIT_DIR=/mnt/f/dev/kor-travel-common/.git/worktrees/review-t104-t020-t021-post2-a`, `GIT_WORK_TREE=/mnt/f/dev/kor-travel-common-wt/review-t104-t020-t021-post2-a`를 설정했다. 저장소 설정은 변경하지 않았다.
- exact 원격 CI: `gh run list --commit f9e8b0494006aebc6b3fa77cdb60c502f50d7182 --json databaseId,headSha,conclusion,status,url`, `gh run view 34167910375 --json headSha,conclusion,jobs`로 [run 34167910375](https://github.com/digitie/kor-travel-common/actions/runs/34167910375)의 head SHA·success를 직접 확인. docs, packages, tools Windows, tools Ubuntu, secret-scan, check-versions **6개 모두 success**.

## 코드 동일성을 확인해 재사용한 검증

`git diff --name-only d91ac02 HEAD -- tools tests .github versions.json packages/tokens`의 유일한 출력은 `packages/tokens/README.md`다. 직전 8427fad→최종 후보는 대비 문서 5파일만 변경됐다. 따라서 다음 시험을 새 후보에서 다시 실행했다고 표시하지 않고, 동일 코드에 대한 이번 연속 리뷰의 결과로 재사용한다.

- d91ac02 Windows Python 3.14.3: `python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` → **238 tests, 67.615s, OK, skip 0**.
- d91ac02 WSL uv Python 3.11.15: `uv run --no-project --with jsonschema --python 3.11 python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` → **238 발견, 41.818s, OK, skip 1**(Windows 8.3 전용), 실제 성공 237건.
- Windows d91ac02·WSL 8427fad에서 외부 reviewer helper `.git/codex-audit/t102-a-package.py`를 Python으로 실행. 임시 복사에서 npm ci → check → build → check → test → pack → 임시 install/export resolve가 완료됐다. 각각 **7 tests, skip 0; pack 19 files, alias 포함/examples 제외; install/export resolve PASS**. Windows Node 25.9.0/npm 11.12.1에는 요구 엔진 `^22.12.0` 경고가 있었으므로 Node 22 로컬 결과로 표시하지 않는다.
- d91ac02의 Python regex 대조: tokens.css root 44개/dark 44개/TK-3 44개 집합 동일, 88개 값 불일치 0. 이번 TK-8 수정은 값 표와 CSS를 바꾸지 않았다.
- d91ac02 Windows `node -` + Playwright Chromium `setContent`/`getComputedStyle`: class/media light에서 앱 brand `oklch(47% 0.14 255)`, dark에서 common brand `oklch(76% 0.085 169)`; scoped radius/brand는 두 모드 모두 1rem/16px/red. 앱 root `--chart-1:purple`은 profile 자식에서도 purple. 앱 차트 미선언은 빈 변수/투명 배경이며 공개 breaking 이관 절과 일치한다. 해당 CSS는 최종 후보와 동일하다.

## NOT_RUN과 기록 경계

- **NOT_RUN**: 실제 T-103 CLI·소비자 LICENSE/metadata 변경·외부 license-only PR·B4 원천 대조·소비자 build/e2e/6폭 visual·npm/PyPI 게시. 외부 G-LIC를 닫지 않았다.
- Windows Node 22 로컬 pack과 Linux 실제 브라우저 CSS 재현은 NOT_RUN. 원격 exact CI, WSL 패키지 시험, Windows Chromium 결과를 구별한다.
- 직전 후보 8427fad의 post-fix-02 보고서는 coordinator가 후보를 교체해 확정하지 않은 초안이다. 이번 최종 판정 원본은 이 post-fix-03 파일이며, 초기·사후 1차 확정 원본을 덮어쓰지 않았다.
- 새 source 파일/테스트를 작성하거나 후보 내용을 고치지 않았다. 패키지 시험의 생성기 호출 뒤에도 후보 Git 내용 변경은 없었다. 소비자 저장소 쓰기·commit/push·다른 reviewer 결과 열람은 하지 않았다.

최종 **PASS**는 누적 A finding 7건 FIXED와 이번 후보의 문서·CSS 계약 정합에 대한 독립 판정이다.
