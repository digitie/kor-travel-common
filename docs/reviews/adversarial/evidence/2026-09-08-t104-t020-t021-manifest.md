# T-104 · T-020 · T-021 적대적 리뷰 manifest

- Review ID: `2026-09-08-t104-t020-t021`
- 검토 기준선: `1eb8a78` (`main` 부모)
- 검토 후보 commit: `7ffe525f8a6cfaef4ca4cbab7017306b1994f24a`
- 후보 branch: `codex/t104-design-tokens`
- 공통 기준: 위 후보 commit의 git object만 검토한다. branch 이동·후속 commit은 이 리뷰의 기준을 바꾸지 않는다.
- 범위: `packages/tokens` 실물에 맞춘 T-104 디자인 토큰 표준과 T-020/T-021 common 측 GPL-3.0-or-later 결정·요청·evidence gate 문서
- 범위 밖: 소비자 저장소 수정, T-103 대비 구현, 소비자 빌드/e2e/visual 실행, npm/PyPI publish, 외부 LICENSE 파일·SHA·CI 증거 자체

## 공통 수용 기준

1. `docs/standards/design-tokens.md`의 44개 변수와 light/dark 값이 `packages/tokens/tokens.css`와 생성물 검사 결과에 맞는다.
2. shadcn alias 4개, admin/consumer profile, override·dark·contrast·format·font·shim 계약이 실제 공개 파일과 모순되지 않는다.
3. 토큰 표준의 정본·task·README·resume 상태가 T-104 리뷰 전 상태를 정확히 표시하고, 링크·계획 DAG가 통과한다.
4. T-020/T-021은 사용자의 GPL-3.0-or-later 결정을 common 문서와 요청 task에 반영하되, 소비자 외부 LICENSE evidence가 오기 전에는 G-LIC gate를 닫지 않는다.
5. T-420/T-454/T-473 등 외부 선행 gate와 실제 task 번호를 혼동하지 않으며, 실행하지 않은 consumer/npm/PyPI 검증을 통과로 표시하지 않는다.
6. SPDX·secret·production-redaction·문서 링크·계획 검사 및 tokens 생성물 검사를 실행 결과와 함께 재현할 수 있다.

## 실행 결과(후보 commit에서 확인한 값)

- `python -B -X utf8 tools/validate_document_links.py` — PASS (`382 documents`, `2351 local targets`, `errors=0`)
- `python -B -X utf8 tools/validate_plan.py` — PASS (`상세 task=106`, `오류=0`)
- `npm run check --workspace packages/tokens` — PASS (`tokens.css 생성물 검사: clean`)
- `python -B -X utf8 tools/check_spdx.py --root .` — PASS (`45개 파일`, `오류 0개`)
- `python -B -X utf8 tools/scan_secrets.py --root . --staged` — PASS (`28개 파일`, `발견 0건`)
- `python -B -X utf8 tools/check_prod_redaction.py --root . --staged` — PASS (`28개 파일`, `발견 0건`)
- `git diff --staged --check` — PASS (후보 commit 직전)
- 미실행: T-103 대비 구현, 소비자 빌드/e2e/visual, npm/PyPI publish — 의도적 `NOT_RUN`

## reviewer 입력과 격리

- Reviewer A: 디자인 시스템·토큰 값·alias·profile·CSS 적용 범위·contrast/link 검토
- Reviewer B: GPL 결정·ADR/task DAG·외부 evidence gate·NOT_RUN·문서 상호참조 검토
- 두 reviewer는 이 manifest와 후보 commit SHA만 받고 상대 원본 report를 받지 않는다.
- 각 reviewer는 후보 object를 `git show`/detached 또는 object-only 방식으로 확인하고 시작·종료 clean 상태와 관찰 hash를 원본 evidence에 남긴다.
- 원본 evidence 경로: `2026-09-08-t104-t020-t021-reviewer-a.md`, `2026-09-08-t104-t020-t021-reviewer-b.md`
- P0/P1은 수정 및 같은 reviewer의 post-fix 재확인 없이는 merge하지 않는다. P2/P3도 owner·task·gate·기한 없이 defer하지 않는다.
