# T-005a 적대적 리뷰 manifest

- 기준 commit: `3bc0f473833aa51a8656b045b705891debf8cf13`
- 기준 base: `6e1881b86f017d604ccfa416bd368e5aba24c669` (PR #5 병합 후 `main`)
- task: T-005a `check_versions.py`의 `uv.lock` parser 확장
- 범위: `tools/check_versions.py`, `tests/test_check_versions.py`, uv fixture 4개, `docs/standards/versions.md`, T-005a 상태·resume 기록
- 범위 밖: `poetry.lock`·`requirements.txt`, consumer 수정·consumer CI, uv 설치/배포·registry 게시, npm·tokens·UI·Python package 실물
- 정본: `docs/standards/versions.md` §3.2·§3.8, `versions.json`, T-005a acceptance
- 주요 계약: uv lock `version = 1`, 지원 revision 0~4, lock `requires-python`, package source 형태, branch `FLOATING_REF`, 버전형 tag 허용, 공유 lock 전이 검사
- 검증: `python -B -X utf8 -m unittest discover -s tests -p 'test_check_versions.py' -v`; 전체 unittest; Windows `py -3.14`; `tools/check_spdx.py`; `tools/validate_document_links.py`; `tools/validate_plan.py`; `git diff --check`; fixture CLI와 airport read-only lock 재현
- 리뷰 격리: 각 reviewer는 기준 commit에 detached worktree를 만들고 시작·종료 `git status --porcelain=v1`가 비어 있는지 확인한다. 서로의 원본 결과는 확정 전 공유하지 않는다.
- 리뷰 A: uv TOML 입력 경계·workspace/member 범위·fail-closed·출력 정보 비공개
- 리뷰 B: 버전 정본·revision/source 호환성·Python 축 판정·문서/테스트 회귀
