# T-005a 독립 full 적대적 리뷰 A 원본

- 실행 ID: `T005A-A-20260907-120232-3bc0f47`.
- 판정: **BLOCK**. 신규 finding 4개(P1 2개, P2 2개), 모두 OPEN.
- 실행 시각: `2026-09-07T12:02:32.5087695+09:00` ~ `2026-09-07T12:09:14.4426766+09:00`.
- candidate: `3bc0f473833aa51a8656b045b705891debf8cf13`; base: `6e1881b86f017d604ccfa416bd368e5aba24c669`.
- 격리: `F:/dev/kor-travel-common-wt/review-t005a-a`의 detached worktree에서 시작·종료 SHA가 candidate와 일치했고 `git status --porcelain=v1 --untracked-files=all`은 비어 있었다. candidate와 소비자 파일은 수정하지 않았다.
- 검토 범위: uv TOML/schema/source, 공유 lock, dependency group, 복수 source, 실패 처리, 출력, Windows/WSL 회귀. AGENTS, 문서 지도, resume, T-005a, 버전 정본, 실제 코드와 시험을 대조했다. 상대 reviewer의 결과와 후속 commit은 읽지 않았다.

## 직접 실행 결과

| 명령·검증 | 결과 |
|---|---|
| `python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` | Windows Python 3.14.3, 139 tests·skip 0, exit 0 |
| `uv run --no-project --python 3.11 python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` | WSL Python 3.11.15, 139 tests·skip 0, exit 0 |
| `tools/validate_document_links.py` | 269문서·2150대상, 오류 0 |
| `tools/validate_plan.py` | task 102개, 오류 0 |
| `tools/check_spdx.py` | 20파일, 오류 0 |
| `tools/scan_secrets.py --all`, `tools/check_prod_redaction.py --all` | 각각 332파일, 발견 0·예외 0 |
| `tools/check_versions.py --self-check` | exit 0; registry 자체 검사로 소비자 검증은 아님 |
| `git diff --check <base> HEAD` | 최초·종료 모두 exit 0 |
| 실제 CLI 추가 공격 | 양 OS 동일 14사례, 아래 반례 재현 |
| 공유 lock 추가 공격 | 명시적 멤버 1개·두 멤버·역순 입력 모두 차단·전이 git 검사 유지 |

추가 공격은 임시 디렉터리와 합성 registry에서 실제 CLI를 호출했다. 정상 대조군은 OK·exit 0, branch 대조군은 FLOATING_REF·exit 1이었다. 오류 표식 원문은 로그에 기록하지 않고 포함 여부만 비교했다. 실제 소비자 build/sync와 원격 CI는 NOT_RUN이다.

## Findings

### A-P1-01 — 고정 선언이 같은 이름의 lock git source 검사를 생략

- 위치: `tools/check_versions.py:970`, `:987` (candidate 기준).
- 재현: `custom-lib`를 버전 tag로 고정 선언하고 lock의 같은 이름 source를 `?rev=main` 또는 복수 항목 중 하나만 SHA로 둔 뒤 `--mode fail` 실행.
- 결과: 양 OS에서 OK만 보고되고 exit 0. 선언별 `resolved` 하나만 확인한 뒤 `name in urls` package를 전이 검사에서 제외한다.
- 영향: marker별 미고정·손상 lock source가 fail 모드를 통과한다.
- 권고: 선언 판정과 모든 lock git entry 판정을 독립 수행하고 누락·짧은 SHA를 별도 실패로 남긴다. 순서 변경 회귀를 둔다.

### A-P1-02 — 알려진 필드의 잘못된 source·구조가 정상 lock으로 통과

- 위치: `tools/check_versions.py:467`, `:478`, `:508` (candidate 기준).
- 재현: registry URL을 `https://[`로 변경하거나 최상위 marker를 정수로 만들거나 package `dependencies`를 dict로 변경. 저장소 없는 git source와 40자 fragment 대조도 실행.
- 결과: 일부 잘못된 입력이 양 OS에서 OK·exit 0이고, 미지 source/정수 registry 대조만 exit 2였다.
- 영향: 지원 형식 검증이 실제 구조와 일치하지 않는다.
- 권고: source URL·경로와 알려진 하위 필드의 자료형을 명시적으로 검사하고 지원하지 않는 구조는 입력 오류로 종료한다.

### A-P2-03 — 잘못된 dependency group과 복수 source 선언을 조용히 무시

- 위치: `tools/check_versions.py:893`, `:909`, `:916` (candidate 기준).
- 재현: 문자열인 group, 비문자열 group 항목, 없는 `include-group`, 비문자열 git, tag+branch 충돌을 정상 lock과 함께 실행.
- 결과: 각각 OK·exit 0으로 무시되거나 `or` 우선순위로 한 ref만 선택됐다.
- 영향: 새로 소비하는 manifest 절의 오류가 정상 보고로 숨겨진다.
- 권고: PEP 735 group과 `tool.uv.sources`의 자료형·include 존재·ref 배타성을 먼저 검증한다. 유효한 중첩 include는 branch의 FLOATING_REF를 유지해야 한다.

### A-P2-04 — uv 입력 오류가 필드명·package 이름을 원문으로 출력

- 위치: `tools/check_versions.py:455`, `:464`, `:479`, main 출력 `:1210` (candidate 기준).
- 재현: 미지 최상위 필드명 또는 package 이름에 합성 access-key 표식을 넣고 source/형식을 깨뜨린다.
- 결과: exit 2이지만 Windows·WSL stdout/annotation에서 표식 포함을 확인했다.
- 영향: 입력 검증 실패가 CI 로그로 원문을 재출력한다. 경로를 포함하는 오류 형식도 같은 경계 위험을 가진다.
- 권고: 오류 종류와 고정 안전 경로만 사용하고 입력 이름·값·민감 경로를 출력하지 않는다. stdout/stderr/report의 비공개 회귀를 둔다.

## 한계와 판정

- NOT_RUN: candidate 원격 CI, 실제 uv sync/build/install, 최신 소비자 상태의 제품 gate, npm/PyPI 게시·merge.
- 실제 airport/weather 소비자 대조는 별도 후속 evidence가 필요하며 합성 fixture가 이를 대체하지 않는다.
- 공식 uv 문서와 고정 upstream source를 읽기 전용으로 대조했다. 전체 resolver 동등성을 주장하지 않는다.
- 최종 판정은 **BLOCK**이며 A-P1-01, A-P1-02, A-P2-03, A-P2-04는 후속 candidate에서 재검토해야 한다.
