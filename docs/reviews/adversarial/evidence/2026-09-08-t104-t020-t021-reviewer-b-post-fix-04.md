# T-104·T-020·T-021 독립 적대적 리뷰 B — post-fix-04

- 실행 ID: `B-T104-T020-T021-POSTFIX04-20260908-075154`
- 최종 판정: **PASS**. 최초 B finding 4건 모두 FIXED, 잔여·신규 P0/P1/P2/P3 finding 0건.
- 연속 검토 시작(KST): `2026-09-08T07:48:44.3653367+09:00`(중간 f9e8b04).
- 최종 후보 검토 시작/종료(KST): `2026-09-08T07:51:54.4968167+09:00` / `2026-09-08T07:52:54.8759267+09:00`.
- 최종 시작·종료 SHA: `2ca6b0c491ddcf542d0c77540b78d18ddf100dfb`.
- 최종 시작·종료 tree: `d07d9fc6a6506d1166a73d556b6eae75710d570b`.
- 직전 문서 delta 기준: `f9e8b0494006aebc6b3fa77cdb60c502f50d7182`; 누적 기준: `7ffe525f8a6cfaef4ca4cbab7017306b1994f24a`.
- 격리: `F:/dev/kor-travel-common-wt/review-t104-t020-t021-post3-b`, detached worktree. f9e8b04에서 clean 확인 후 부모 요청으로 2ca6b0c에 전환했다. 최종 후보 시작·종료 `git status --porcelain=v1` 출력 없음.
- source `.git/config` 시작·종료 SHA256: `26B889B3EB290BA6E5A8BEAFE330E3CEAF47832CD202324162640DD12AD862D7`, 변경 없음. 시작 시 기본 checkout의 이전 B post-fix-02 원본이 untracked였으며 보존했다.
- 상대 reviewer 원본·결과는 읽거나 요청하거나 공유하지 않았다. 기존 원본과 후보·소비자 파일 수정, commit, push, 게시를 수행하지 않았다. 이번 작성 파일은 이 원본 하나다.

## 범위와 이전 원본

부모는 8427fad 이후 일반/상태 텍스트 4.5:1, 비텍스트 3:1, muted 읽기 표면의 조건부 추가를 반영한 f9e8b04를 요청했고, 이어 조사 재현 오차와 합격 판정을 분리한 2ca6b0c로 기준을 교체했다. f9e8b04의 별도 최종 원본은 생성하지 않았다. 이 판정은 2ca6b0c에만 적용한다.

[최초 B 원본](2026-09-08-t104-t020-t021-reviewer-b.md), [post-fix 원본](2026-09-08-t104-t020-t021-reviewer-b-post-fix.md), [post-fix-02 원본](2026-09-08-t104-t020-t021-reviewer-b-post-fix-02.md)의 자기 finding과 누적 수정 상태를 확인했다. `8427fad..f9e8b04`의 5개 문서 변경, `f9e8b04..2ca6b0c`의 3개 문서 변경을 전부 읽고 관련 TK-8·T-103·T-104·style-delivery·CHANGELOG 문장과 대조했다. 변경 없는 코드 및 앞선 범위는 Git 동일성을 확인하여 독립 검증 결과를 재사용했다.

## 누적 finding disposition

| ID·원 심각도 | 판정 | 최종 후보의 근거 |
|---|---|---|
| B-P1-01 / P1 | **FIXED** | 소비자별 license-only 외부 PR → T-021 해당 행의 LICENSE/main SHA/검사 evidence → 코드 채택 순서가 유지된다. B4·common provenance·소비자 build/quality는 선행 LICENSE PR의 수용 기준에서 제외됐다. |
| B-P2-02 / P2 | **FIXED** | T-453/T-454/T-485는 concierge 행, T-472/T-473/T-486은 docker-manager 행을 참조한다. T-420은 pinvi 외부 반영 담당으로 구별된다. |
| B-P2-03 / P2 | **FIXED** | DONE은 common 결정·요청·evidence 형식 정의까지이며 외부 B4를 수행한 것으로 표시하지 않는다. 요청서의 인벤토리·판정 링크와 외부 NOT_RUN/OPEN이 유지된다. |
| B-P3-04 / P3 | **FIXED** | resume와 상세 task의 완료 20·열린 86·전체 106이 일치한다. |

추가로 부모가 전달한 WCAG 일반/상태 텍스트 P1과 조사 오차 경계를 독립 재검증했다. 상대 원본 ID나 보고서는 제공받지 않았으며 임의로 상대 finding ID를 붙이지 않았다. 아래 내용으로 해당 우려가 해소됐다고 판단한다.

## WCAG 및 임계값 반례

2026-09-08에 [W3C WCAG 2.2 SC 1.4.3 공식 설명](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)과 [SC 1.4.11 공식 설명](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html)을 직접 조회했다. 일반 텍스트에는 최소 4.5:1이 적용되고, 비활성 UI·순수 장식 등에는 별도 예외가 있다. 비텍스트 정보·컨트롤의 관련 대비 기준은 3:1이다. SC 1.4.3은 기준 미만을 반올림하여 합격시키지 않도록 설명한다.

최종 TK-8:112–121, T-103:21, style-delivery:98과 T-104 수용 기준을 대조했다. primary/secondary/strong/tertiary 및 tint 위 상태 텍스트는 4.5:1, icon/control-line/focus/tint mark는 3:1로 구별된다. muted는 일반 읽기 표면의 기본 집합에서 빠지지만 실제 읽기 배경으로 쓰는 앱은 해당 4.5:1 쌍을 추가해야 한다. 색 이름을 이유로 실제 읽기 텍스트를 면제하는 규약은 아니다.

독립 최소 계산 반례는 회색 `#777777`과 흰색 `#ffffff`다.

```python
s = 119 / 255
luminance = ((s + 0.055) / 1.055) ** 2.4
ratio = 1.05 / (luminance + 0.05)
print(ratio, ratio >= 4.5, ratio >= 4.5 - 0.03, round(ratio, 1) >= 4.5)
```

Windows Python 실제 결과는 `4.478089453577214, False, True, True`였다. 즉 정확한 4.5 판정에서는 미달이고, 0.03을 임계값에서 빼거나 한 자리로 반올림하면 잘못 통과한다. 최종 후보는 TK-8·T-103에 조사 수치 대조 오차를 합격 임계값에 적용하지 말 것과 반올림 합격 금지를 명시하고, T-104 수용 기준에도 이를 추가했다. 따라서 이 반례는 문서상 반드시 미달로 분류된다.

이 결과는 새 검사기 실행 결과가 아니다. `tools/kt_contrast.py`는 아직 없으며 T-103 구현은 NOT_RUN이다. baseline 예외나 report-only 실행의 성공도 WCAG 적합성 합격과 동일하게 보지 않는다.

## 실행·동일성 검증

| 명령·검사 | 결과·귀속 |
|---|---|
| `git rev-parse HEAD 'HEAD^{tree}'`, `git status --porcelain=v1` | 최종 SHA/tree 일치, detached clean |
| `git diff --stat 8427fad f9e8b04`, `git diff --stat f9e8b04 HEAD` 및 관련 전체 diff | 각각 문서 5개, 문서 3개. 최종 delta는 3줄 추가·2줄 삭제 |
| `py -3.14 -B -X utf8 tools/validate_document_links.py` | 최종 2ca6b0c 문서 387·대상 2,375, 오류 0 |
| `py -3.14 -B -X utf8 tools/validate_plan.py` | 최종 task 106, 오류 0 |
| `git diff --check 7ffe525 HEAD` | 최종 exit 0 |
| `git diff --exit-code 8427fad f9e8b04 -- packages tools tests .github versions.json AGENTS.md` 및 `git diff --exit-code f9e8b04 HEAD -- packages tools tests .github versions.json AGENTS.md` | 모두 exit 0. 두 문서 후보에서 코드·시험·CI·버전·AGENTS 불변 |
| `py -3.14 -B -X utf8 F:/dev/kor-travel-common/.git/codex-audit/review-t102-post-b-gates.py F:/dev/kor-travel-common-wt/review-t104-t020-t021-post3-b` | f9e8b04의 독립 임시 사본에서 SPDX 45 오류 0, secrets/prod-redaction 각 502개 발견·예외 0, versions self-check 성공, alias CSS 1개 오류 0, focused 35 tests / 0.943초 / OK·skip 0. 최종 3줄 문서는 직접 검토하고 불변 구현 검증을 재사용 |
| sRGB 대비 산술 반례 | 최종 후보 검토 중 실제 Python 실행, 4.478089453577214 확인 |
| `gh run list --commit 2ca6b0c491ddcf542d0c77540b78d18ddf100dfb --limit 2 --json databaseId,headSha,status,conclusion,url` | exact SHA [run 34168063255](https://github.com/digitie/kor-travel-common/actions/runs/34168063255), completed/success |

f9e8b04 CI 34167910375도 성공으로 관찰했지만 최종 SHA의 CI로 대체하지 않았다. 이전 package check·npm 7 tests·Python 전체 238 tests는 변경 없는 해당 코드의 기존 결과를 재사용하며 이번 신규 전체 실행으로 세지 않는다.

## 미실행·소비자 경계

이번 WSL/Linux 로컬 시험, 브라우저 computed-style·소비자 build/e2e·시각 대비, 외부 LICENSE/main SHA 실측, 실제 T-103 검사기, tarball 설치, npm/PyPI 게시는 **NOT_RUN**이다. LICENSE-only 외부 gate의 OPEN과 공용 GPL-3.0-or-later·원천 고지·소비자 쓰기 금지·미게시 경계를 유지한다. chart 슬롯 앱 소유의 Breaking/Migration 안내와 공개 README/architecture 계약은 이전 수정 상태를 유지한다.

최초 B finding은 모두 FIXED이고, 추가 대비·임계값 검토에서 잔여 또는 신규 finding은 없다. 최종 2ca6b0c 독립 리뷰 판정은 **PASS**다.
