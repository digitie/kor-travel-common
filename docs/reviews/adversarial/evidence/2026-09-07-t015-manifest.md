# T-015 독립 리뷰 manifest

- Candidate: `a28c2a726f6940bf9f287188b87b1d5c12a8cd55`.
- Base: `659aa6dd3cb319761e8f7290155e025b11029c83`.
- 범위: base부터 candidate까지 42개 문서 전체 delta. npm/PyPI 미게시·식별자 확정, common 구현 선행과 외부 gate 분리, 후보 보존·다음 minor·release branch 정책을 대조한다.
- 정본: [T-015](../../../tasks/T-015-common-delivery-plan.md), [ADR-014](../../../adr/014-common-implementation-without-registry-publishing.md), [packages](../../../architecture/packages.md), [release](../../../runbooks/release.md), [workflow](../../../runbooks/agent-workflow.md). 이전 ADR-005·013의 변경되지 않은 조항도 확인한다.
- 사용자 범위: common만 변경, 모든 구현 순차 진행, npm/PyPI 미게시, 검증·두 리뷰·CI 뒤 PR 병합. 기존 common GPL-3.0-or-later와 원천 고지는 유지한다.
- 격리: 각각 detached worktree에서 실제 SHA와 시작·종료 clean을 확인한다. 두 원본 확정 전 상대 결과를 읽지 않는다. 원본만 기본 checkout의 지정 evidence 파일에 기록하고 검토 중인 트리는 수정하지 않는다.
- 영역: A는 정보 정본·agent 실행 가능성·task DAG·registry 제외 누락. B는 후보 버전·자산·태그 불변성·release branch·CI·외부/권리 gate·false DONE/PASS. 두 reviewer 모두 전체 delta를 확인한다.
- 검증: Python 3.11+ 문서 link·plan·전체 unittest, base diff --check. 변경 task 경로를 실제 agent처럼 추적하고 이전 minor 보존 후 다음 minor 진행·늦은 rc 발행·산출물 유실·미승인 소비자·registry 부재 시나리오를 공격한다. 구현 코드가 없는 패키지의 동작을 성공으로 추정하지 않는다.
- 범위 밖: 패키지 구현·빌드·설치, 실제 소비자 실행·저장소 쓰기, npm/PyPI 조회·게시·이름 예약, 실제 GitHub Release·태그 생성. 미실행 항목은 NOT_RUN으로 기록한다.

## 공통 요청 원문

위 candidate/base에서 독립 full 적대적 리뷰를 수행한다. 문서 수가 많다는 이유로 전체 문서를 무작정 통독하지 말고 42개 delta와 관련 정본을 대조한다. 계획의 모순·누락·숨은 외부 선행·후보 덮어쓰기·미실행 성공 처리를 공격한다. 코드나 다른 작업자의 변경을 고치지 말고 지정한 새 reviewer 원본 파일만 작성한다. 실행 ID·전문 영역·시작/종료 시각·전달 입력·실제 SHA·clean·실행 명령과 결과·미검토 범위·위치/근거/실패 시나리오/권고를 갖춘 원 finding ID와 P0~P3 심각도·BLOCK/CONDITIONAL/PASS를 남긴다. 상대 reviewer의 이번 결과는 읽지 않는다. 원본 마지막에는 개행 하나만 둔다.
