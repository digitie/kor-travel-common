# T-010 독립 적대적 리뷰 A 원본

- 실행 ID: `A-T010-20260909-071625-b6192fb`.
- 리뷰어: `/root/t010_review_a`.
- 불변 후보 commit: `b6192fb189cf1b4a288f02f9a675e51bfe0dbc7d`.
- 후보 tree: `84077320ede9010eaca4ced2ecc8d98d156296f1`.
- delta base: `ddb4b7479c39d049688a095a0febe7dfd12e02c3`.
- 기준선 tree: `d62c387c3ebd590d25c882a7b474f451de377145`.
- 격리 경로: `F:/dev/kor-travel-common-wt/review-t010-a` detached worktree.
- manifest: review 요청에 사용한 inline manifest v1.
- manifest SHA-256(UTF-8/LF): `bce7d315b78b95d0b013289063f33f56a09c9d068afa5e2964f61d2e7108c566`.
- 시작·종료 `git status --porcelain=v1`: 빈 출력.
- 코드 수정·commit·push: 없음. 상대 reviewer 결과는 읽지 않음.

## 요청과 최종 판정

T-010 재사용 workflow, fixture, consumer pin과 smoke 입력을 입력 경계·계약·재현성 관점에서 독립 공격했다.

**최종 verdict: BLOCK** — P0 0건, P1 4건, P2 2건.

## Finding

### A-T010-P1-01 — `docs/` 입력이 정본 예시와 충돌

- 위치: `.github/workflows/docs-check.yml:86-96`, 정본 예시 `docs/standards/ci-deploy.md:78`.
- `redaction-scope: docs/`는 `raw.split("/")` 마지막 빈 요소 때문에 경로 안전성 검사에서 거부된다. selftest는 trailing slash 없는 경로만 사용한다.
- 문서의 복사 가능한 호출 예시가 workflow에서 실패한다.
- trailing slash를 정규화한 뒤 검증·실행하고 `docs/` 양성 fixture를 추가해야 한다.

### A-T010-P1-02 — fork PR에서 selftest common checkout 실패

- 위치: `.github/workflows/workflows-selftest.yml:36,44,50,60,70`.
- fork PR의 `github.event.pull_request.head.sha`는 fork 저장소에만 존재하는데 이를 `digitie/kor-travel-common`의 `common-ref`로 전달한다. called workflow의 두 번째 checkout이 해당 SHA를 찾지 못한다.
- 동일 저장소 branch/push와 fork PR의 결과가 달라지며 fork PR의 selftest 6개 job이 실패한다.
- caller checkout 재사용 경로 또는 명시적 selftest checkout 우회 입력이 필요하다.

### A-T010-P1-03 — candidate asset URL이 선택한 pin 저장소와 묶이지 않음

- 위치: `.github/workflows/consumer-smoke.yml:61-90`, `tools/consumer_smoke.py:133-141,188-192`.
- `map-tokens` 선택 후에도 `digitie/kor-travel-airport` release URL이 형식 검사를 통과한다. URL은 `digitie` release와 `.tgz` 형식만 확인하고 selected pin repository와 비교하지 않는다.
- package name과 digest만 맞는 다른 저장소 artifact를 map consumer에 설치해 잘못된 smoke 결과를 만들 수 있다.
- selected pin URL과 asset URL의 repository를 정확히 비교하고 source/digest를 registry 또는 attestation에 묶어야 한다.

### A-T010-P1-04 — 오타 난 `repo`가 report 모드로 조용히 downgrade됨

- 위치: `.github/workflows/versions-check.yml:13-17,85-90`, `tools/check_versions.py:2927-2933`.
- `repo: kor-travel-mpa`처럼 미등록 값을 전달하면 checker 기본값 `report`로 BELOW_FLOOR가 있어도 exit 0이다.
- production consumer의 repo 오타가 version gate를 비활성화한다.
- 미등록 fixture 허용을 별도 명시 입력으로 분리하고 기본은 registry key/alias 외 입력을 거부해야 한다.

### A-T010-P2-05 — `lockfiles` 목록의 실제 항목을 checker가 사용하지 않음

- 위치: `.github/workflows/versions-check.yml:49-77,94-111`.
- workflow는 항목 존재만 확인한 뒤 parent directory를 checker에 전달한다. `README.md`도 파일이면 통과하며 해당 디렉터리 전체를 재귀 검사한다.
- 선언된 lockfile과 실제 검사 범위가 달라지고 unselected scope가 섞인다.
- 허용된 lockfile 종류를 검사하고 정확한 lockfile/manifest scope를 checker에 전달해야 한다.

### A-T010-P2-06 — consumer smoke가 Node/npm 실행 환경을 고정하지 않음

- 위치: `.github/workflows/consumer-smoke.yml:47-50,115-139`, 설치 실패 fixture `tests/test_consumer_pins.py:123-125`.
- `setup-node`가 없고 runner ambient Node/npm에 의존한다. fixture는 `--engine-strict`인데 실제 workflow `npm install`에는 해당 정책이 없다.
- runner 이미지 변경과 unsupported engine warning이 smoke 결과를 바꿀 수 있다.
- SHA 고정 `setup-node`, Node/npm 버전, 동일한 engine 정책을 사용해야 한다.

## 실행 결과

- `python -B -X utf8 -m unittest tests.test_consumer_pins -v`: 6 OK.
- node/python fixture `check_versions.py`: report mode, 각각 exit 0, BELOW_FLOOR 확인.
- 문서 링크: 516 documents / 2564 targets, 오류 0.
- redaction fixture 2종: 오류 0.
- 대비 light/dark fixture: exit 0.
- workflow YAML 파싱: 성공.
- exact map/weather/airport pinned SHA fetch: 성공.
- `actionlint`: `NOT_RUN(actionlint 미설치)`.
- 전체 unittest 329개: `node_modules/@mdx-js/mdx` 미설치로 38 failures, 3 errors. 전체 gate 통과로 집계하지 않음.
- `git diff --check`: 오류 0.
