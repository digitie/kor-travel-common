# T-104·T-020·T-021 독립 적대적 리뷰 B

- 실행 ID: `B-T104-T020-T021-20260908-070743`
- 최종 판정: **BLOCK** — P0 0건, P1 1건, P2 2건, P3 1건. 아래 4건은 이 후보에서 미해결이다.
- 검토자: `/root/reviewer_b`. 상대 reviewer의 이번 결과를 읽거나 요청하거나 공유하지 않았다.
- 시작(KST): `2026-09-08T07:07:43.9761433+09:00`
- 검토 종료(KST): `2026-09-08T07:15:19.0080316+09:00`
- 시작·종료 candidate: `7ffe525f8a6cfaef4ca4cbab7017306b1994f24a`
- 시작·종료 tree: `c0442fdab1f40a5bec8948091a5b1f60003be4bb`
- parent/base: `1eb8a78b0fe8ec890b5d15b9f4abfd3f81ef9d59`
- 격리: `F:/dev/kor-travel-common-wt/review-t104-t020-t021-b` detached worktree. 시작·종료 `git status --porcelain=v1` 출력은 모두 비어 있다.
- 기본 checkout `.git/config` 시작·종료 SHA256: `26B889B3EB290BA6E5A8BEAFE330E3CEAF47832CD202324162640DD12AD862D7`. 설정을 수정하지 않았다.
- 작성 소유 범위: 이 원본 보고서 한 파일. 후보·기존 문서·소비자 파일 수정, commit, push, 게시를 수행하지 않았다.

## 요청과 기준선

요청은 immutable candidate에서 GPL-3.0-or-later 결정/ADR, task DAG, 요청 문서, T-420/T-454/T-473 외부 evidence gate, 실제 task 번호, NOT_RUN, 문서 상호참조와 소비자 경계를 독립 검증하는 것이다. 공통 manifest는 [2026-09-08-t104-t020-t021-manifest.md](2026-09-08-t104-t020-t021-manifest.md)이며 읽은 파일의 SHA256은 `7405566889F01F35D54ADA842BB2B93A58B36EBF5955A60D575123473DB1E511`이다.

Manifest는 candidate tree에는 없고 기본 checkout에서 별도로 전달된 요청으로 읽었다. 이후 branch commit을 구현 기준에 섞지 않았다. `git diff HEAD^ HEAD`의 변경 28개는 모두 Markdown이다. `tools`, `tests`, `packages`, `versions.json`, `.github`에는 변경이 없다. AGENTS, 문서 라우터와 resume, T-104/T-020/T-021 및 관련 외부 task, ADR-004/010, architecture·licensing·요청 문서의 관련 계약과 전체 변경 범위를 대조했다.

## 실행 검증

명령의 기준 디렉터리는 위 detached worktree다. 시험 helper는 기존 `.git/codex-audit`의 실행기를 재사용했다. 이 실행기는 후보를 독립 임시 디렉터리로 복사하고 Git 관련 환경을 제거한 뒤 그 사본만 초기화한다. source checkout의 Git 설정을 시험 저장소로 사용하지 않았다.

| 실제 실행 명령·검사 | 결과 |
|---|---|
| `git rev-parse HEAD 'HEAD^{tree}'`, `git status --porcelain=v1` | 시작·종료 SHA/tree 일치, clean |
| `git diff --name-only HEAD^ HEAD` | Markdown 28개 |
| `git diff --exit-code HEAD^ HEAD -- tools tests packages versions.json .github` | exit 0, 코드·시험·패키지·버전·CI 불변 |
| `git diff --check HEAD^ HEAD` | exit 0 |
| `py -3.14 -B -X utf8 F:/dev/kor-travel-common/.git/codex-audit/review-t102-post-b-gates.py F:/dev/kor-travel-common-wt/review-t104-t020-t021-b` | exit 0, 아래 정적 검사와 focused 시험 실행 |
| `tools/validate_plan.py` | 상세 task 106개, 오류 0 |
| `tools/validate_document_links.py` | 문서 382개, 링크 대상 2,351개, 오류 0 |
| `tools/check_spdx.py` | 검사 45개, 오류 0 |
| `tools/scan_secrets.py --all` | 파일 497개, 발견 0, 예외 0 |
| `tools/check_prod_redaction.py --all` | 파일 497개, 발견 0, 예외 0 |
| `tools/check_versions.py --self-check` | exit 0 |
| aliases checker 및 focused unittest | CSS 1개 오류 0, 35 tests / 0.767초 / OK |
| `py -3.14 -B -X utf8 F:/dev/kor-travel-common/.git/codex-audit/review-t102-b-wsl-unittest.py F:/dev/kor-travel-common-wt/review-t104-t020-t021-b` | Windows Python 3.14 전체 238 tests / 55.178초 / OK, skip 없음. Helper 이름과 달리 이번 호출은 Windows 실행이다. |
| `npm run check --workspace packages/tokens` | exit 0, 생성물 일치 |
| Python 읽기 전용 표 대조 | 토큰 표 이름 44개, CSS 이름 44개, 누락·추가 없음 |
| Python 읽기 전용 task 상태 집계 | DONE 20, BLOCKED 79, READY 6, IN_PROGRESS 1; 전체 106, 열린 86 |
| PROVENANCE 원천 저장소 열 대조 | 원천이 pinvi인 행 0개 |
| `gh run list --commit 7ffe525f8a6cfaef4ca4cbab7017306b1994f24a --limit 3 --json databaseId,headSha,status,conclusion,url` | exact SHA run `34165490138`, completed/cancelled. CI 성공으로 세지 않음 |

Focused 35개와 전체 238개는 중복을 포함하므로 서로 더해 독립 시험 수로 표시하지 않는다. 계획 validator의 성공은 task metadata DAG 검증 결과이며, 다음 finding의 본문에 숨어 있는 PR 선행 조건을 증명하지 않는다.

## B-P1-01 — 라이선스 선행 PR과 코드 채택 PR의 연결이 순환한다

- 심각도: **P1**. Disposition: **미해결, 수정 후 재검토 필요**.
- 위치: `docs/plan/requests/docker-manager-license-l8.md:17`, `docs/tasks/T-472-ktdm-tokens-adoption.md:56,76`, `docs/tasks/T-473-ktdm-ui-partial.md:6,77`, `docs/tasks/T-453-concierge-theme-tokens.md:55`, `docs/tasks/T-454-concierge-ui-v02.md:6,55,75`.
- 최소 재현: 요청서에서 `rg -n '분리하지' docs/plan/requests/docker-manager-license-l8.md`, task 디렉터리에서 `rg -n -g 'T-453*' -g 'T-454*' -g 'T-472*' -g 'T-473*' '선행:|외부 LICENSE|PR을 열지' docs/tasks`를 실행하고 순차 착수 조건을 대조했다.
- 관찰: docker-manager 요청은 common 토큰·UI·Python 링크 변경과 LICENSE PR을 분리하지 말라고 요구한다. 하지만 T-472와 T-473은 외부 LICENSE evidence 없이는 PR을 열지 못하고, T-473은 T-472 완료를 선행으로 둔다. LICENSE-only PR으로 먼저 이 조건을 해제하는 방법은 요청서 17행을 위반한다.
- concierge도 T-453 PR 3 완료를 위해 **T-454 PR 링크**를 요구하지만 T-454는 T-453을 선행으로 두고 LICENSE evidence 이전에는 열지 못한다. 새 요청서의 표현은 별도 LICENSE PR을 T-454의 선행 evidence로 기록한다는 뜻으로도 읽히지만, 상세 task의 수용 기준은 여전히 T-454 PR 자체를 지명한다. 원장 위치와 evidence를 생산하는 PR을 구분하지 않았다.
- 영향: 다른 선행이 전부 완료되어도 문서를 그대로 따르는 순차 에이전트가 L8 해제 PR과 최초 코드 채택 PR을 합법적인 순서로 연결할 수 없다. 임의로 본문 gate를 무시하거나, 아직 없는 후행 PR을 선행 승인 evidence로 대체해야 한다. metadata DAG 검사에서는 이 본문 순환을 잡지 못한다.
- 권고: 소비자별 LICENSE-only 외부 PR/request를 코드 채택과 독립하여 먼저 수행할 수 있게 정의한다. evidence 생산 PR과 기록할 task를 구별하고, T-453/T-472/T-485/T-486에는 해당 소비자의 외부 LICENSE PR·main merge SHA를 선행으로 둔다. T-454/T-473 코드 채택 task는 그 evidence를 소비하게 한다. common의 소비자 쓰기 금지는 유지한다.

## B-P2-02 — 소비자별 라이선스 evidence에 다른 저장소 task 번호가 섞여 있다

- 심각도: **P2**. Disposition: **미해결, 수정 필요**.
- 위치: `docs/tasks/T-472-ktdm-tokens-adoption.md:56`, `docs/tasks/T-485-concierge-py-first.md:54`.
- 최소 재현: `rg -n 'T-454/T-473' docs/tasks/T-472-ktdm-tokens-adoption.md docs/tasks/T-485-concierge-py-first.md` 및 T-454/T-473 제목·대상 저장소를 대조했다.
- 관찰: docker-manager의 T-472와 concierge의 T-485가 모두 `T-454/T-473 PR`을 LICENSE evidence로 요구한다. T-454는 concierge, T-473은 docker-manager다. 이번 변경에서 종전 T-021 참조를 두 번호의 공통 문자열로 바꿨다.
- 영향: 서로 다른 소비자의 승인 링크를 복사해 수용 기준을 채운 것으로 읽힐 여지가 있으며, manifest의 소비자별 실제 task 번호·evidence 구분 조건을 만족하지 못한다. 현재 canonical G-LIC가 OPEN이라는 사실까지 잘못 승인됐다고 주장하는 것은 아니다.
- 권고: 각 파일에는 해당 소비자의 evidence만 명시하고 저장소·LICENSE 경로·main merge SHA를 결합한다. B-P1-01의 별도 선행 PR 모델과 함께 consumer별 단일 목적지를 기록한다.

## B-P2-03 — T-020/T-021을 DONE으로 닫으면서 요청 산출물의 기존 수용 기준을 남겼다

- 심각도: **P2**. Disposition: **미해결, 완료 범위 정합 수정 필요**.
- 위치: `docs/tasks/T-021-ktc-ktdm-license-l8.md:3,24-25,39,56`, `docs/tasks/T-020-pinvi-license-l6.md:24,38`, `docs/plan/requests/concierge-license-l8.md:17,31`, `docs/plan/requests/pinvi-license-l6.md:32`, `docs/plan/requests/docker-manager-license-l8.md:31`.
- 최소 재현: T-021의 `B4|diff 결과|상태`를 검색하고 요청서 전문을 읽었다. 각 요청의 Markdown 링크를 T-020/T-021의 요청 문서 수용 기준과 대조했다.
- 관찰: T-021 구현 3번과 수용 기준은 실제 B4 diff 판정 결과를 요청서와 PROVENANCE 후보 행에 기록하도록 요구한다. 요청서는 이를 앞으로 `diff로 확인`하라고 지시할 뿐 결과를 담지 않으며, T-021 evidence는 B4 diff를 외부 T-454/T-473으로 넘긴다. 그런데 상태는 DONE이다. 또한 세 요청서에는 수용 기준에 지정된 인벤토리 §8/§9 및 판정 §3.1의 직접 링크가 없다. task로 가는 간접 링크는 존재하나 요구된 요청서 산출물의 링크와 같지 않다.
- 영향: 사용자 GPL 결정이 확정됐다는 사실과 필요한 산출물까지 충족됐다는 사실이 섞인다. 다음 에이전트는 닫힌 task에 남은 B4 조건을 완료 여부와 무관하게 넘겨야 할지 다시 실행해야 할지 결정해야 한다. 링크 검사 통과도 누락된 필수 링크를 보증하지 않는다.
- 권고: common 결정·요청만 닫으려는 사용자 범위에 맞춰 B4의 실제 조사 결과를 외부 후속 gate로 명시적으로 이관하고 T-021 구현·수용 기준·evidence를 일치시킨다. common에서 B4를 완료한 것처럼 기록하지 않는다. 요청서에 요구된 인벤토리·판정 링크도 추가한 뒤 DONE 범위를 확정한다. 이 finding은 소비자 파일을 지금 수정하라는 요청이 아니다.

## B-P3-04 — resume의 현재 완료 수치가 두 개다

- 심각도: **P3**. Disposition: **미해결, 문서 수치 정정 필요**.
- 위치: `docs/resume.md:7,11`.
- 최소 재현: `rg -n '완료 18|완료 20' docs/resume.md`; 상세 task의 `- 상태:`를 읽기 전용 Python으로 집계했다.
- 관찰: 7행은 완료 20·열린 86, 11행은 현재 상세 task 106·완료 18·열린 88이라고 한다. 실제 후보 상세 task는 완료 20·열린 86이다.
- 영향: 재개 문서의 현재 상태를 두 가지로 전달한다. 이번 T-020/T-021 완료 기록을 반영하는 과정에서 과거 수치가 남았다.
- 권고: 현재 상태 수치를 한 곳에서 선언하고 다른 문장은 그 상태를 가리키도록 정리한다.

## 확인한 경계와 미실행

- GPL-3.0-or-later, §7 추가 허가 없음, pinvi 공개 방향은 이번 사용자 결정의 common 정본 표기로 일관되게 기록됐다. ADR·licensing은 결정 완료와 실제 소비자 LICENSE 반영을 구별하고 G-LIC를 OPEN으로 남겼다. geo의 기존 -only 고지와 제3자 고지를 일괄 삭제하거나 pinvi 코드를 common으로 복사하는 변경은 없다. 법률 자문 또는 외부 권리 확인을 수행한 것으로 표시하지 않는다.
- T-420은 외부 pinvi PR을 기다리는 BLOCKED 상태다. T-104는 IN_PROGRESS이고 T-103 구현·소비자 대비/시각 검증을 이번 문서 수정 성공으로 대체하지 않았다. 토큰의 44개 이름과 생성물 일치는 직접 확인했지만 모든 브라우저 계산값·시각 대비를 새로 검증한 것은 아니다.
- `NOT_RUN`: 소비자 LICENSE 실물/40자리 merge SHA/외부 PR 검증, 소비자 build/e2e·시각·품질 검사. 이번 요청의 외부 선행이며 G-LIC 해제 evidence로 계산하지 않는다.
- `NOT_RUN`: 이번 후보의 WSL/Linux 전체 시험, 실제 npm tarball 설치·PyPI 빌드·registry 게시. 코드·패키지·CI는 parent와 동일하지만 과거 실행을 이번에 새로 수행했다고 기록하지 않는다. Windows 전체 시험과 패키지 생성물 check만 위 표의 실제 실행이다.
- exact candidate CI는 [run 34165490138](https://github.com/digitie/kor-travel-common/actions/runs/34165490138)이 cancelled다. 이후 commit CI를 이 SHA의 성공으로 대체하지 않았다.
- 조회 중 후보에 없는 manifest 경로와 잘못 확장한 PowerShell wildcard/요청서 이름 조회는 실패했고 올바른 명시 경로로 다시 읽었다. 이를 검증 성공으로 세지 않았다.

최종적으로 코드 회귀 검증과 링크·metadata DAG 검사는 통과했지만, 순차 실행 계약과 DONE 산출물의 미해결 P1/P2 때문에 이 후보의 문서 gate는 BLOCK이다.
