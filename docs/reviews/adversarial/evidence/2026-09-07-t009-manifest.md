# T-009 독립 리뷰 manifest

- Candidate: `45c238842a0b8b842ba63e9347b2f7218a3b2f0f`.
- Base: `82dec2b939885863100802997f9e7548dffd3c9a`.
- 범위: 전체 PR delta. CI 하드닝·두 OS tools·secret/prod 검사기·정책·회귀·fixture report, 관련 규약·보안 정정·task evidence를 대조한다.
- 정본: [T-009](../../../tasks/T-009-ci-hardening.md), [CI 규약](../../../standards/ci-deploy.md), [개발 환경](../../../dev-environment.md), [workflow](../../../runbooks/agent-workflow.md), [라이선스](../../../standards/licensing.md).
- 사용자 범위: common만 작성, 순차 구현, npm/PyPI 게시·이름 조회 제외, 독립 리뷰 2인과 CI 뒤 PR 병합. 실제 Release·태그·소비자 변경·원격 ruleset 변경은 범위 밖이다.
- 격리: 각각 detached worktree에서 실제 SHA와 시작·종료 clean을 확인한다. 검토 트리는 수정하지 않고 기본 checkout의 지정 reviewer 원본만 작성한다. 상대의 이번 결과는 두 원본 확정 전 읽지 않는다.
- 영역: A는 스냅샷 선택·정책 파서·입력 오류·정보 비공개·탐지 경계·회귀. B는 CI trigger/ref/SHA·권한·Windows·report 증거·고지·redaction 정책·branch protection. 두 reviewer 모두 전체 delta를 확인한다.
- 검증: 문서 link·plan·전체 unittest·SPDX·두 guard의 전체 트리·base diff check. 빈 범위, index/HEAD와 작업 트리 불일치, 경로·인코딩·link·정책 오류, report의 실제 판정과 source 연결, PR/main/release push에서 누락·오인 성공을 공격한다.
- 보안: base의 survey 삭제행에는 기존 민감 값이 있다. raw survey diff나 과거 원문을 로그에 출력하지 않는다. coordinator가 제공한 로컬 마스킹 helper로 전체 diff를 읽고 현재 placeholder와 치환 횟수를 대조한다. 원본 결과에도 원문 값이나 탐지되는 평문 시험 값을 적지 않는다. 필요한 재현 값은 분할해서 생성한다.
- 현재 로컬 증거: Windows 131 tests·skip 0, WSL Python 3.11의 131 tests·skip 0, link260/2107·plan101·SPDX18·guard319파일 오류 0. 실제 PR CI·임시 release push는 coordinator가 별도 실행 중이며 이 manifest에서 PASS로 세지 않는다.

## 공통 요청 원문

위 candidate/base에서 독립 full 적대적 리뷰를 수행한다. 동작하는 정상 경로에 머물지 않고 명시 계약의 실패·우회·오인 성공을 재현한다. 코드와 다른 작업자의 변경을 수정하지 말고 지정한 새 reviewer 원본만 작성한다. 실행 ID·전문 영역·시작/종료 시각·전달 입력·실제 SHA·clean·명령/결과·미검토 범위와 원 finding ID, P0~P3 심각도, 위치·근거·영향·권고 및 BLOCK/CONDITIONAL/PASS를 기록한다. 상대 reviewer의 이번 결과는 읽지 않는다. 파일 끝에는 개행 하나만 둔다.
