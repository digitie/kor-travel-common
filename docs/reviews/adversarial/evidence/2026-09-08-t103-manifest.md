# T-103 적대적 리뷰 매니페스트

이 매니페스트는 두 reviewer가 같은 immutable candidate를 독립적으로 검사하기 위한 기준선이다. reviewer 간 finding과 결론은 최종 보고 전까지 공유하지 않는다.

| 항목 | 값 |
|---|---|
| task | T-103 |
| candidate commit | `d12ccba48f67ad8ac863dbc97d85dda289c1e09c` |
| candidate tree | `598d18a7ad69c6780e17df780c245fe960cfc0af` |
| branch/PR | `codex/t103-kt-contrast-ux-lint` / draft PR #20 |
| 기준일 | 2026-09-08 |
| working tree | candidate commit checkout, untracked·staged 변경 없음 |

## 리뷰 범위

- `tools/kt_contrast.py`: CSS scope·OKLCH/hex/alpha 계산·필수 TK-8 쌍·baseline/만료/출력·Windows/stdlib 경계
- `tools/ux_lint.py`: P1~P8·패턴 경계·주석/백틱·root/token allowlist·제외 경로·baseline/diff·출력/redaction
- `tests/test_kt_contrast.py`, `tests/test_ux_lint.py`, `tests/fixtures/ux/*`
- 네 앱 오버라이드·baseline 예제, 템플릿·도구 문서·T-103 evidence와 상태 동기화

## 고정 검증 명령

```text
python -B -X utf8 -m unittest discover -s tests -p "test_*.py" -v
python -B -X utf8 tools/validate_document_links.py
python -B -X utf8 tools/validate_plan.py
python -B -X utf8 tools/check_spdx.py
python -B -X utf8 tools/scan_secrets.py --all
python -B -X utf8 tools/check_prod_redaction.py --all
git diff --check d12ccba48f67ad8ac863dbc97d85dda289c1e09c^ d12ccba48f67ad8ac863dbc97d85dda289c1e09c
```

리뷰어는 consumer 저장소·registry·workflow dispatch를 호출하지 않는다. 미실행 gate는 `NOT_RUN(사유)`로 남기며 통과로 세지 않는다. finding은 P0~P3와 `FIXED`/`OPEN`/`ACCEPTED` disposition을 사용하고, P0/P1 또는 재현 가능한 계약 위반이 있으면 NO-GO로 판정한다.
