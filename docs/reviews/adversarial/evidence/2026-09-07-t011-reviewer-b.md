# T-011 독립 적대적 리뷰 B 원본

최종 verdict: **BLOCK**. P1 3건·P2 2건 OPEN이다. 정상 회귀와 CI 성공은 아래 반례의 통과를 의미하지 않는다. 이 원본은 다른 reviewer 결과를 읽거나 공유받기 전에 독립 확정했다.

## 기준선과 격리

- 실행 ID: `T011-B-20260907-192842-KST`.
- candidate: `7f715b0efb7c422f0b6212eea612f0c95dcf6fff`.
- parent/base: `3977c00a5b4ffa21a8d29f96dd9531efbe3a0cb5`.
- tree: `31d205d5ed4ae81f46aff65a899fc794fe100193`.
- detached worktree: `F:/dev/kor-travel-common-wt/review-t011-b`.
- 시작: `2026-09-07T19:28:42.8580440+09:00`. 종료: `2026-09-07T19:36:04.0208034+09:00`.
- 시작·종료 `git status --porcelain=v1` 출력은 비었으며 SHA/tree가 일치했다. 후보 파일·소비자 저장소를 수정하지 않았고 commit/push/게시를 하지 않았다. 별도 임시 디렉터리에 합성 입력을 만들고 검사했다.
- 공통 manifest: 주 checkout `docs/reviews/adversarial/evidence/2026-09-07-t011-manifest.md`, SHA256 `E962CF4B4E2824E81E955363C2F19BFCC4778DF607678C0EBD4EE5F6101263F7`. 이 파일은 검토 시작 때 untracked였으므로 candidate blob이라고 표현하지 않는다. 시작·종료 읽은 해시는 같다.

## 전달 요청 원문

> T-011 독립 적대적 리뷰를 수행해 주세요. 기준 commit은 정확히 7f715b0efb7c422f0b6212eea612f0c95dcf6fff, parent/base는 3977c00a5b4ffa21a8d29f96dd9531efbe3a0cb5입니다. 다른 reviewer 결과를 읽거나 공유하지 말고 detached worktree 또는 object-only 방식으로 검토하세요. 전문 영역은 backend/tool 정합성·경로 보안·문서 정본·CI/라이선스 evidence입니다. 특히 strict validator와 draft schema drift, `check_versions` root/manifest 조합·중복/escape/symlink, report exit semantics, 표준 문서 명령, SPDX·redaction·NOT_RUN·Windows/Linux evidence를 공격하세요. 실행 가능한 검증과 실패 재현을 포함해 P0-P3 finding, 위치·영향·권고·verdict(BLOCK/CONDITIONAL/PASS)를 독립 원본으로 보고하세요. review manifest는 docs/reviews/adversarial/evidence/2026-09-07-t011-manifest.md이며 기준선이 깨끗한지 SHA/tree와 시작·종료 status를 기록하세요. 결과는 제가 다른 reviewer에게 보여주기 전에 확정할 수 있게 자체적으로만 작성하세요.

## 실제 검증

전체 23파일 delta, 신규 validator/schema/초안/회귀, 변경된 check_versions 경로와 연결된 npm 판정·workflow 검색·출력, T-011·versions·consumer-adoption·D-19·라이선스 범위를 대조했다.

| 명령·검증 | Windows Python 3.14.3 | WSL Python 3.11.15 |
|---|---|---|
| `python -B -X utf8 -m unittest discover -s tests` | 188 tests, 80.493초, OK, skip 0 | 188 tests, 33.649초, OK, skip 0 |
| `tools/validate_plan.py` | task 106, 오류 0 | 동일 |
| `tools/validate_document_links.py` | 문서 328·target 2,275, 오류 0 | 동일 |
| `tools/check_spdx.py` | 파일 32, 오류 0 | 동일 |
| `tools/scan_secrets.py --all` | 파일 418, 발견 0, 예외 0 | 동일 |
| `tools/check_prod_redaction.py --all` | 파일 418, 발견 0, 예외 0 | 동일 |
| `tools/check_versions.py --self-check` | exit 0 | exit 0 |
| 직접 CLI·schema·경로 반례 | 아래 결과 재현 | CLI·경로 결과 동일; jsonschema 외부 엔진 미설치 |

Windows 명령은 `py -3.14 -B -X utf8`, WSL 명령은 `/home/digitie/.local/share/uv/python/cpython-3.11-linux-x86_64-gnu/bin/python3.11 -B -X utf8`을 사용했다. WSL 첫 전체 실행은 reviewer가 설정한 `GIT_DIR`/`GIT_WORK_TREE`가 시험의 임시 Git 저장소로 전파되어 188개 중 19 error가 났다(65.226초). 이 실행은 성공으로 세지 않았다. 해당 변수와 `GIT_COMMON_DIR`를 `env -u`로 제거해 위 전체 결과를 다시 얻었다. 후보 상태는 두 실행 뒤 clean으로 확인했다. WSL 읽기 전용 validator에만 Linux 경로의 Git 환경을 전달했다.

- `git diff --check HEAD^ HEAD`: exit 0.
- Windows jsonschema 4.26.0의 `Draft202012Validator.check_schema` 성공, 초안 10/10 schema 통과. 각 초안의 stdlib CLI 통과는 전체 회귀의 `test_all_initial_manifests_pass`에서도 실행됐다.
- `gh run list --commit <candidate>` 및 `gh run view 34111464534 --json headSha,conclusion,jobs`: exact candidate, 5개 job 모두 success. [CI 실행](https://github.com/digitie/kor-travel-common/actions/runs/34111464534). CI 성공을 반례 해결로 세지 않는다.
- `.github`·`versions.json`·`LICENSE`·`NOTICE`·`PROVENANCE.md`는 base 대비 변경 없음. 신규 Python 파일의 GPL 헤더를 직접 확인했다. JSON 헤더 면제는 licensing LIC-17과 일치한다.
- 재현 스크립트: 주 checkout `.git/codex-audit/review-t011-b-cases.py`. 실행: `py -3.14 -B -X utf8 <script> F:/dev/kor-travel-common-wt/review-t011-b`, WSL에서는 두 경로를 `/mnt/f/...`로 전달했다. 원문 비밀은 쓰지 않고 분할 생성한 합성 값의 출력 포함 여부만 boolean으로 기록했다.

## B-P1-01 — strict 오류 및 scope 보고가 입력 비밀을 다시 출력함

- 심각도: P1. disposition: OPEN, 수정 필요.
- 위치: `tools/manifest_schema.py:46`, `:123`, `:145`; `tools/validate_manifest.py:31`; `tools/check_versions.py:2770` 및 `:1812`의 scope label 전달.
- 최소 재현: 정상 example 객체에 `sensitive = 'gh' + 'p_' + 'A' * 36`으로 만든 합성 문자열을 미지 property 이름으로 추가한다. `validate_manifest.py`는 exit 1이지만 stdout에 그 문자열을 그대로 출력한다. root와 manifest를 함께 준 `check_versions.py`는 exit 2지만 annotation에 같은 원문을 출력한다.
- 정상 키인 `lockfiles[0].scope`에 같은 합성 문자열을 넣으면 strict 검증을 통과한다. 정상 Node/npm/React fixture와 `--mode fail --json <out> --markdown <out>` 및 `GITHUB_STEP_SUMMARY`를 주면 exit 0이며 stdout·JSON·Markdown·step summary **4개 채널 모두** 문자열 포함이 true다. scope의 기존 출력 경로는 이전 코드에도 있었지만 새 strict 입력 경로도 이를 허용한다. 미지 property 오류 노출은 이번 신규 validator에서 발생한다.
- 영향: 검사 실패·성공 어느 경로로도 입력의 자격증명 형식 문자열을 CI 로그나 artifact에 재게시할 수 있다. 저장소 자체의 secret scan 성공은 외부 입력에 대한 이 동작을 막지 않는다.
- 권고: 오류는 고정 schema 경로·오류 코드·미지 필드 개수로 구성하고 미지 key 원문은 숨긴다. label을 포함한 입력 문자열의 검사·출력 정책을 전 채널에 일관되게 적용한다. 정책 일치 입력은 본문을 출력하기 전에 입력 오류로 닫거나, 허용되는 표시 계약을 명시하고 안전한 대체값을 쓴다.

## B-P1-02 — lock 옆 manifest symlink가 소비자 root 밖 파일을 읽음

- 심각도: P1. disposition: OPEN, 수정 필요.
- 위치: `tools/check_versions.py:1815`–`:1821`.
- 최소 재현: 임시 `repo/package-lock.json`은 실제 root 내부 정상 파일로 두고 `repo/package.json`만 root 밖 `outside-package.json`을 가리키는 symlink로 만든다. strict manifest의 lock path는 `package-lock.json`, scope는 `root`다. `check_versions.py <repo> --manifest <repo>/manifest.json --mode fail`을 실행한다.
- Windows/WSL 모두 외부 package의 Node 선언을 읽고 보고했으며 정상 외부 값이면 **exit 0**이었다. `scopes_from_manifest`는 lock 자체를 resolve/containment 검사하지만 동반 `package.json`/`pyproject.toml`은 `is_file()`만 검사한다. pyproject 경로에도 같은 코드 결함이 있으며 이번 직접 symlink 실행은 package.json으로 했다.
- 음성 대조: manifest 자체가 root 밖, lock symlink가 root 밖, `.github/workflows` symlink가 root 밖인 경우는 각각 exit 2로 차단됐다.
- 영향: 표준 root 경계의 일부만 보호되어 보고서가 소비 저장소의 고정 입력 대신 외부 파일 내용으로 정상 판정될 수 있다.
- 권고: 실제 읽는 동반 manifest의 resolved 경로도 같은 root 아래인지 검사한다. npm workspace 멤버 manifest와 Python 동반 파일까지 포함하고, 허용하지 않는 symlink는 보고서 생성 전 exit 2로 닫는다.

## B-P1-03 — npm workspace를 선택해도 root package.json을 검사함

- 심각도: P1. disposition: OPEN, 수정 필요.
- 위치: `tools/check_versions.py:1813`–`:1817`; `templates/manifests/map.lock.json:9`, `templates/manifests/pinvi.apps-web.lock.json:9`.
- 최소 재현: root `package.json`의 Node는 `>=24.0.0`, `apps/web/package.json`의 Node는 `>=18.0.0`으로 둔다. 두 곳 React와 shared lock의 설치본은 모두 19.2.8이다. strict manifest는 app/scope `apps/web`, path `package-lock.json`으로 준다.
- Windows/WSL 모두 scope.workspace는 `apps/web`이나 scope.manifest는 root package.json이었다. 보고된 Node 선언은 `>=24.0.0`·OK이고 멤버의 `>=18.0.0`은 누락된다. 최종 4행 모두 OK, **fail 모드 exit 0**이다.
- 영향: `docs/standards/versions.md:48`의 멤버 package.json 별도 범위 계약을 지키지 않아 멤버 runtime/declaration drift를 놓친다. 초기 map·pinvi web 초안도 workspace selector를 요구하는 task 대응표와 달리 scope가 `root`여서 앱 선언 검사 의도를 전달하지 못한다.
- 권고: shared lock 기준 workspace 경로의 package.json을 실제 선언 원천으로 선택하고 containment를 적용한다. root scope 의미는 유지한다. 해당 두 초안의 scope를 실제 앱 workspace 경로로 맞추고, root가 정상이어도 멤버만 floor 미달인 CLI 회귀를 넣는다.

## B-P2-04 — review 날짜와 상대 경로의 schema/validator 계약이 다름

- 심각도: P2. disposition: OPEN, 수정 필요.
- 위치: `tools/manifest_schema.py:129`–`:133`; `templates/kor-travel-common.lock.schema.json:8`–`:18`, `:118`–`:119`, `:129`–`:130`; `docs/tasks/T-011-consumer-manifest-schema.md:17`.
- 최소 재현: 유효한 key 예외에서 `review`만 `tomorrow` 또는 `2026-02-30`으로 바꾸면 stdlib validator와 CLI가 exit 0이다. T-011은 until/review 모두 유효한 ISO 날짜로 정했으나 review에는 문자열 검사만 한다.
- Windows Draft202012Validator 대조: `review=tomorrow`는 schema가 거부하므로 validator와 반대다. `until=2026-02-30`은 schema가 통과시키지만 stdlib은 거부한다. lock path `C:/outside/package-lock.json`, `apps//package-lock.json`, `apps/`도 schema는 통과시키고 stdlib은 거부한다.
- 영향: 같은 v1이 도구에 따라 승인·거절되어 IDE/schema 검사와 CI 결과가 다르며 유효하지 않은 review 일정이 승인된다.
- 권고: review에도 실제 날짜 검증을 적용하고 schema에 날짜·정규 상대 경로 제약을 맞춘다. JSON Schema format의 검증 모드를 명시하고 양쪽에 동일한 정상/음성 corpus를 적용한다. registry 소비자 key와 같은 외부 데이터 의존 검증은 별도 의미 제약임을 문서화한다.

## B-P2-05 — 배열/객체 kind 입력에서 strict validator가 예외로 종료함

- 심각도: P2. disposition: OPEN, 수정 필요.
- 위치: `tools/manifest_schema.py:172`–`:174`; `tools/validate_manifest.py:27`.
- 최소 재현: 정상 manifest의 `lockfiles[0].kind`를 `[]` 또는 `{}`로 바꾼다. `entry['kind'] not in LOCKFILE_KINDS`가 unhashable `TypeError`를 발생시킨다.
- Windows/WSL 모두 `validate_manifest()`가 오류 목록을 반환하지 않고 예외를 던지며 CLI는 traceback을 출력한다. CLI 종료값 자체는 1이지만 정상 형식 오류 계약인 오류 목록·안정된 진단을 제공하지 않는다. check_versions는 상위 TypeError catch로 exit 2 처리하므로 해당 경로의 성공 우회라고 주장하지 않는다.
- 영향: JSON 타입 하나로 공용 validator API가 중단되고 batch 검증/호출자가 구조화된 오류 결과를 얻을 수 없다.
- 권고: enum membership 전에 문자열 타입을 검사해 고정 field 오류를 추가한다. 모든 JSON 타입에 대해 예외 없이 오류 목록을 반환하는 회귀를 둔다.

## 추가 관찰과 한계

- 중복 repo JSON key는 마지막 값으로 해석되며 validator와 check_versions 모두 정상 마지막 값이면 exit 0이었다. 동일 lock/scope 두 항목도 허용되고 최종 행은 병합됐다. 이번 계약에 중복 거부가 명시되지 않았으므로 별도 P0–P3 finding으로 집계하지 않는다. strict 소비자 형식에서 중복 key를 금지할지 정책을 명시하는 것이 좋다.
- 정상 lock이 있고 workflow 디렉터리가 없는 fixture는 정상 버전 행만 낸다. 빈 workflow를 별도 evidence로 표시하라는 T-011 수용 기준은 완료 기록 전에 직접 확인해야 한다. 이번 5건의 finding에 추가 집계하지 않았다.
- NOT_RUN: 실제 소비자 7개 저장소 빌드·e2e·매니페스트 설치, 공용 패키지 실물, 릴리스 tag/asset, npm/PyPI 게시. 요청 범위 밖이며 소비자에 쓰지 않았다.
- NOT_RUN: WSL의 외부 jsonschema 엔진 대조(모듈 없음). Windows에서는 실제 엔진으로 실행했고 WSL stdlib·CLI 반례는 전부 재현했다.
- CI는 exact head와 job/step 상태를 읽었으며 전체 원격 로그의 모든 줄을 재감사하지 않았다. 실제 auth 구현·권리 재선언·외부 서비스 검증은 수행하지 않았다.

위 P1 3건과 P2 2건을 수정한 immutable 후보로 post-fix 재검토가 필요하다. 본 candidate는 BLOCK이다.
