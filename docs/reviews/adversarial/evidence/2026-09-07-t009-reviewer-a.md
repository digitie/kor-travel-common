# T-009 독립 적대적 리뷰 A 원본

- 실행 ID: `T009-A-20260907-091739-45c2388`
- reviewer: `reviewer_a`; 우선 영역: 검사기 스냅샷·정책 파서·실패 처리·정보 비공개·탐지 경계·Windows/Linux 회귀. 전체 delta도 검토했다.
- 판정: **BLOCK**. 신규 finding 4개(P1 2개, P2 2개). 현재 원본의 대상은 아래 immutable candidate이며 후속 수정의 성공을 이 후보의 성공으로 소급하지 않는다.
- 시작: `2026-09-07T09:17:39.8404947+09:00`; 검사 종료: `2026-09-07T09:28:00.2985018+09:00`.
- base: `82dec2b939885863100802997f9e7548dffd3c9a`; candidate: `45c238842a0b8b842ba63e9347b2f7218a3b2f0f`.
- 격리: `F:/dev/kor-travel-common-wt/review-t009-a`, detached HEAD. 시작·종료 `git rev-parse HEAD` 모두 candidate와 일치하고 `git status --porcelain=v1 --untracked-files=all`은 모두 빈 출력이다.
- manifest: [공통 입력](2026-09-07-t009-manifest.md). 이번 상대 reviewer 원본·메시지는 읽거나 요청하지 않았다. 제품·정책·소비자·다른 evidence는 수정하지 않았다. 재현 파일은 자동 정리되는 임시 Git 저장소에만 작성했다.

## 전달 요청 원문

> 새 T-009 독립 full 리뷰를 시작하세요. 공통 manifest: F:/dev/kor-travel-common/docs/reviews/adversarial/evidence/2026-09-07-t009-manifest.md. Candidate 45c238842a0b8b842ba63e9347b2f7218a3b2f0f / base 82dec2b939885863100802997f9e7548dffd3c9a. 격리 트리 F:/dev/kor-travel-common-wt/review-t009-a (이미 detached 생성). A 우선 영역은 검사기 스냅샷/정책 파서/실패 처리/정보 비공개/탐지 경계/회귀이며 전체 delta도 확인. 원본 소유 파일은 기본 checkout docs/reviews/adversarial/evidence/2026-09-07-t009-reviewer-a.md 하나입니다. 다른 작업자가 있으므로 다른 파일을 수정하거나 되돌리지 마세요. 상대 이번 결과는 읽지 마세요. 중요: base survey 삭제행의 기존 운영 값이 출력되면 안 됩니다. 전체 diff는 기본 checkout에서 `python -B -X utf8 .git/codex-audit/t009-review-diff.py <base> <candidate> -- <paths>`로 마스킹 후 읽으세요. helper는 과거 값이 포함되는 survey/T306만 마스킹하고 나머지 전체 diff는 보존합니다. raw survey 과거 내용 출력 금지. 현재 후보 문서는 직접 읽어도 됩니다. 재현 및 report에 평문 시험 비밀/사설 IP를 쓰지 말고 문자열을 분할 생성하세요. 실제 PR/release CI는 제가 병행 중이며 로컬 성공을 CI 성공으로 세지 마세요. 새로운 발견을 독립적으로 재현하고 원본 확정 후 알려주세요.

manifest의 공통 요청도 적용했다.

> 위 candidate/base에서 독립 full 적대적 리뷰를 수행한다. 동작하는 정상 경로에 머물지 않고 명시 계약의 실패·우회·오인 성공을 재현한다. 코드와 다른 작업자의 변경을 수정하지 말고 지정한 새 reviewer 원본만 작성한다. 실행 ID·전문 영역·시작/종료 시각·전달 입력·실제 SHA·clean·명령/결과·미검토 범위와 원 finding ID, P0~P3 심각도, 위치·근거·영향·권고 및 BLOCK/CONDITIONAL/PASS를 기록한다. 상대 reviewer의 이번 결과는 읽지 않는다. 파일 끝에는 개행 하나만 둔다.

## 실행·대조 결과

코드·문서 30파일의 전체 delta(732행 추가·55행 삭제)를 지정 helper로 읽었다. `AGENTS.md`·문서 라우터·resume·T-009, CI·버전·라이선스 정본과 관련 runbook을 대조했다. 현재 정책과 scanner 전체, 두 신규 시험 모듈과 report fixture, 기존 checker의 관련 경로 처리를 직접 읽었다. survey 과거 내용은 stdout에 출력하지 않았다. 기존 시험의 실패 메시지도 합성 값을 노출하지 않도록 전체 출력을 캡처한 뒤 시험 건수·성공 여부만 출력했다.

| 실제 명령·검사 | 결과 |
|---|---|
| Windows Python 3.14.3: `python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` | 131 tests, skip 0, 83.908초, exit 0 |
| WSL Ubuntu-26.04: `uv run --no-project --python 3.11 python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` | Python 3.11.15, 131 tests, skip 0, 28.145초, exit 0 |
| `python -B -X utf8 tools/validate_document_links.py` | 260문서·2108대상, 오류 0 |
| `python -B -X utf8 tools/validate_plan.py` | task 101개, 오류 0 |
| `python -B -X utf8 tools/check_spdx.py` | 18파일, 오류 0 |
| `python -B -X utf8 tools/scan_secrets.py --all` | 319파일, 발견 0·예외 0, exit 0 |
| `python -B -X utf8 tools/check_prod_redaction.py --all` | 319파일, 발견 0·예외 0, exit 0 |
| `git diff --check 82dec2b939885863100802997f9e7548dffd3c9a HEAD` | 최초·종료 검사 모두 빈 출력, exit 0 |
| Windows 정책·입력 추가 공격 12개 | 기대 일치 11개, 깊은 regex 1개 불일치(A-P2-03) |
| 깊은 regex 실제 CLI의 WSL 재현 | exit 1·`RecursionError`·traceback, 합성 표식 출력 없음 |
| report step의 실제 Python 블록 재실행 | 보고서는 exit 0·finding 3개·BELOW_FLOOR 1개, 후속 assertion은 exit 1(A-P1-01) |
| Windows `GetShortPathNameW` 별칭 경로 재현 | 정규 경로 성공, 8.3 별칭은 `ValueError`(A-P1-02) |
| 고정 report fixture에 이동 workflow 참조를 추가한 checker 실행 | report exit 0, 기존 finding 3개, FLOATING_REF 0·workflow finding 0(A-P2-04) |

12개 추가 공격은 `version=true`, 미지 스키마 필드, 중복 규칙 ID, 잘못된 allowlist 형식·정수 path, 잘못된 regex, 깊은 regex, UTF-8 BOM의 합성 비밀, 정책 경로 탈출, 잘못된 base ref, 줄바꿈 뒤 따옴표 값, stage된 rename이다. 기존 `RepositoryCase`로 임시 Git 입력을 만들고 실제 scanner CLI를 subprocess로 호출했다. 합성 표식은 조각을 합쳐 생성했고 출력에 값이 포함되는지는 boolean으로만 확인했다. 깊은 regex를 제외한 오류 입력은 exit 2, 탐지 입력은 exit 1이며 표식 유출은 없었다.

스냅샷 선택에 대해서는 추가 공격과 기존 실행 시험을 함께 대조했다. `--all`은 작업 파일과 정책, `--staged`는 index blob과 index 정책, `--base`는 HEAD blob과 HEAD 정책을 읽고, 빈 범위·읽기/인코딩/링크 오류를 성공으로 처리하지 않는다. regex 범위가 모든 업무 비밀을 보증하지 않는다는 현재 정본의 명시적 한계를 탐지 누락과 구분했다.

survey 보안 정정은 base/current 내용을 메모리에서만 비교해 네 placeholder의 증가 횟수를 출력했다. `cross/ci-deploy.md`는 3/3/5/0, `cross/version-matrix.md`는 0/0/1/0, airport는 3/0/3/0, geo는 0/1/0/1, weather는 2/0/3/0으로 README §9의 사설 주소/내부 호스트/운영 도메인/접속 자격증명 기록과 모두 일치했다. T-306의 예시 치환도 마스킹된 diff로 대조했다.

## Finding

### A-P1-01 — 다른 step에 쓴 source SHA를 현재 step summary에서 요구해 필수 CI가 실패함

- 심각도: **P1**, disposition: **OPEN**.
- 위치: `.github/workflows/docs.yml:143`(관련 127행).
- 재현: 임시 `GITHUB_STEP_SUMMARY`를 현재 report step의 fixture 설명만으로 시작한다. 실제 `tools/check_versions.py`를 고정 fixture에 `--mode report --json <임시 보고 경로>`로 실행한 뒤, candidate workflow의 `import hashlib`부터 이어지는 Python 블록을 그대로 추출해 실행했다. 보고서에는 finding 3개·BELOW_FLOOR 1개가 있고 현재 summary에도 표가 있지만 `SOURCE_SHA`는 없다. assertion은 exit 1이다.
- 원인·영향: SHA를 쓴 127행은 앞선 별도 step이다. GitHub는 `GITHUB_STEP_SUMMARY` 파일을 step마다 다르게 제공하므로 현재 143행 조건은 정상 보고에서도 거짓이다. PR·main·release push의 필수 `check-versions` job을 차단한다. [GitHub 공식 summary 설명](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-commands#adding-a-job-summary), 조회 2026-09-07, step별 파일 및 step 격리 절과 대조했다.
- 권고: report step에서 source를 기록하고 그 step의 생성물을 검증하거나, 명시적 공용 evidence 파일을 사용한다. 서로 다른 step summary 파일을 사용하는 회귀 시험과 실제 PR·release push 검증이 필요하다.

### A-P1-02 — Windows 8.3 별칭의 정상 manifest 경로가 새 필수 tools job을 깨뜨림

- 심각도: **P1**, disposition: **OPEN**.
- 위치: `tools/check_versions.py:557`, `tools/check_versions.py:562`; 새 Windows 실행 경로는 `.github/workflows/docs.yml`의 `tools` matrix.
- 재현: 임시 디렉터리에 `repo`와 npm `lockfiles` 항목을 가진 manifest를 만들되 선택 필드 `scope`는 생략한다. 정상 경로를 `scopes_from_manifest`에 전달하면 성공한다. Windows `GetShortPathNameW`로 같은 디렉터리의 별칭을 구해 같은 manifest를 전달하면 `ValueError`가 발생한다. 이번 환경에서 별칭이 실제로 달라졌음을 확인했고 경로 내용은 출력하지 않았다.
- 원인·영향: `base`는 별칭을 유지하지만 각 입력은 `.resolve()`로 정규화한다. 정규 경로의 `path.parent.relative_to(base)`가 실패한다. 이는 기존 도구에 있던 결함이지만 새 mandatory Windows CI와 정상 manifest 입력을 직접 막는 T-009 회귀다. 로컬 전체 시험 성공은 임시 경로 형식이 다른 hosted Windows 성공을 보증하지 못한다.
- 권고: manifest 기준 디렉터리와 해석된 파일 경로에 같은 정규화를 적용한다. 별칭 또는 symlink/junction 경유의 정상 manifest 회귀를 추가하고 실제 Windows Python 3.11 CI로 확인한다.

### A-P2-03 — 깊은 정책 정규식의 컴파일 오류가 exit 2 계약을 벗어남

- 심각도: **P2**, disposition: **OPEN**.
- 위치: `tools/_scan.py:142` 및 `tools/_scan.py:192`의 예외 처리.
- 재현: 임시 Git 저장소의 정책에 `version=1`, 단일 `TEST-RULE`을 쓰고 regex를 `"(" * 700 + ("review-" + "private-" + "marker") + ")" * 700`으로 생성한다. 정상 텍스트 파일과 정책을 index에 넣은 뒤 candidate `tools/scan_secrets.py --root <임시 저장소> --all`을 실행한다. Windows Python 3.14.3과 WSL Python 3.11.15에서 모두 exit 1, `RecursionError`, traceback을 확인했다. 합성 표식은 출력되지 않았다.
- 원인·영향: `re.compile`에서 발생하는 `RecursionError`는 `re.error`도 현재 바깥 예외 목록도 아니다. 입력/정규식 오류를 안전한 진단과 exit 2로 분리하는 `ci-deploy.md` §8.1 계약을 어기고, 탐지 결과가 없는 실행을 발견 exit 1로 오분류한다. 실패를 성공으로 통과시키지는 않으며, 이 재현에서 비밀 원문 유출도 없으므로 P2로 한정한다.
- 권고: 해당 컴파일 실패를 일반화된 `ScanError`로 바꾸거나 정규식 구조 제한을 검증한다. 양 wrapper에 적용되는 정책 오류 회귀에서 exit 2·traceback 없음·원문 없음이 확인돼야 한다.

### A-P2-04 — 버전 정본이 T-009에 약속한 workflow 참조 검사가 실제 범위에서 빠져 있음

- 심각도: **P2**, disposition: **OPEN**.
- 위치: `docs/standards/versions.md:96`, `docs/standards/versions.md:171`; 관련 §7 `actions` 행, `docs/tasks/T-009-ci-hardening.md` 구현 범위·수용 기준.
- 재현: 고정 version-report fixture를 임시 디렉터리에 복사하고 `.github/workflows/fixture.yml`에 common reusable workflow의 `@main` 참조를 추가했다. candidate checker를 그 루트에 `--repo common-ci-report-fixture --mode report --json <임시 보고서> --no-step-summary`로 실행하면 exit 0, 기존 finding 3개만 출력되고 FLOATING_REF와 workflow finding은 0이다. 코드에도 YAML/workflow 탐색·파서가 없다.
- 원인·영향: 버전 정본 §3.8은 reusable workflow 참조를 T-009 검사 대상에 넣는다고 선언하고 §8도 GitHub Actions·CI Node 미검사의 후속을 T-009로 지목한다. 상세 task는 현재 common workflow의 SHA 핀과 기존 checker 실행만 요구한다. 현행 workflow 자체의 핀은 맞더라도 T-009를 닫을 때 약속된 자동 탐지의 소유 task가 사라진다. 이는 현행 YAML이 안전하지 않다는 주장이 아니라 종료 후 실행 책임과 검사 보증의 충돌이다.
- 권고: 자동 탐지를 이번 범위에 실제 포함하거나, 명시적인 후속 task·수용 기준을 만들고 버전 정본의 T-009 약속과 범위를 함께 정정한다. 현재 parser가 해당 입력을 검사한다고 기록해서는 안 된다.

## 검증 한계와 최종 판정

- **NOT_RUN(독립 원격 CI 조회·실행 없음)**: 실제 PR/main/release run과 hosted runner의 검증은 coordinator가 병행했다. coordinator는 원 candidate의 PR `34069260693`·release push `34069314480` 실패 및 후속 수정 `ec34d6a`의 PR `34069558960`·release push `34069558276` 성공을 전달했다. 이는 상대 reviewer 결과가 아닌 coordinator 관찰이며 본인의 로컬 검증과 구분한다. 후속 수정 코드는 이번 기준선에 없으므로 FIXED로 판정하지 않았다.
- **NOT_RUN(사용자·manifest 범위 밖)**: 소비자 실행/변경, npm/PyPI 게시, 태그·GitHub Release 생성, 원격 ruleset 변경. 체크 이름·read 권한·SHA pin·사건별 checkout·timeout·concurrency는 candidate workflow와 정본을 정적으로 대조했다.
- **NOT_RUN(새 후속 범위)**: YAML parser의 구현, 정규식이 임의의 모든 비밀 형식을 탐지하는지에 대한 보증. 문서에 선언된 경계 및 실제로 생성한 실패 입력만 판정했다.
- 사소한 잘못된 시험 파일명 조회 1회는 실제 `tests/test_scan_secrets.py` 경로를 다시 찾아 읽었으며 검증 성공으로 집계하지 않았다.
- 최종 **BLOCK**. 현재 candidate의 P1 두 건과 P2 두 건에 disposition이 필요하다. 후속 candidate에서 수정 delta와 원 finding ID를 유지한 두 독립 재검토가 필요하며, 로컬 131 tests 성공은 위 반례를 무효화하지 않는다.
