# T-003 Reviewer A 독립 적대적 리뷰 원본

- Review ID: `2026-09-07-t003`
- 실행 ID: `/root/reviewer_a` / `t003-a-20260907T063410+0900`
- 전문 영역: SPDX 검사기·회귀 시험·입력/파일 경계·오탐/누락·Windows/Linux·기존 도구 영향
- 시작: `2026-09-07T06:34:10.4874537+09:00`
- 검토 종료: `2026-09-07T06:38:45.5683110+09:00`
- Candidate: `017fef1ca0aa8ca28d0ccf8cfc35fffa71149e64`
- Base: `b36c99fb6c7f4df2a27364842ee197fc947e461e`
- 격리: `F:/dev/kor-travel-common-wt/review-t003-a` detached worktree. 시작·종료 실제 HEAD가 candidate와 일치하고 `git status --porcelain=v1`은 모두 빈 출력이었다.
- 독립성: 상대 reviewer의 원본·메시지·finding을 읽거나 요청하지 않았다. 이 파일 하나만 main evidence에 작성했다. 제품·정책·기존 원본·다른 에이전트 변경·소비자 저장소를 수정하지 않았고 추가 subagent를 만들지 않았다.
- 최종 verdict: **BLOCK**. A-T003-P1-01·A-T003-P1-02·A-T003-P2-03 모두 OPEN.

## 전달 요청 원문과 범위

> T-003 새 독립 적대적 리뷰를 진행하세요. 공통 manifest는 F:/dev/kor-travel-common/docs/reviews/adversarial/evidence/2026-09-07-t003-manifest.md 입니다. Candidate 017fef1ca0aa8ca28d0ccf8cfc35fffa71149e64, base b36c99fb6c7f4df2a27364842ee197fc947e461e. 사용자가 전체 구현 task 순차 진행과 common 외 저장소 수정 금지를 명시했습니다. A 역할: check_spdx.py·회귀 시험·실패 경계·누락/오탐·Windows/Linux·기존 도구 영향. F:/dev/kor-travel-common-wt/review-t003-a detached에서만 기준선 코드/문서를 읽고 시험하세요. 시작/종료 SHA 및 clean 상태 확인. 반대 reviewer의 원본/메시지를 읽지 말고 결과를 독립 확정하세요. 자기 원본 F:/dev/kor-travel-common/docs/reviews/adversarial/evidence/2026-09-07-t003-reviewer-a.md 하나만 작성할 책임입니다. 공유 저장소에 다른 에이전트가 있으므로 다른 변경을 되돌리거나 제품/문서를 수정하지 마세요. 실행 ID·시작/종료 시각·이 요청 원문·scope·검증·미검증·심각도/위치/재현/영향/권고·PASS/CONDITIONAL/BLOCK 포함. 실패 시나리오를 실제로 공격하고, 초안이나 작성자 evidence를 맹신하지 마세요. PR은 common draft #2입니다.

[공통 manifest](2026-09-07-t003-manifest.md), AGENTS·문서 지도·resume·T-003 전체, licensing의 검사 계약·ADR-004·PROVENANCE와 관련 변경을 읽었다. 50개 변경 파일 목록과 코드·시험·설정의 전체 diff를 확인했다. 원문 사본은 로컬 21개 digest를 대조했으며 원격 법률 원문/배포물의 재확보 검증은 이 reviewer의 범위에 포함하지 않았다. 코드 복사 진위·법적 해석 자체보다 저장소가 명시한 검사 계약의 실제 강제 여부를 판단한다.

## 정상 검증과 기존 회귀

| 실행 환경·명령 | 실제 결과 |
|---|---|
| Windows `python --version` | Python 3.14.3 |
| Windows `python -B -X utf8 tools/check_spdx.py` | exit 0, 13개 파일·오류 0 |
| Windows `python -B -X utf8 tools/validate_document_links.py` | exit 0, 215개 문서·1836개 local target·오류 0 |
| Windows `python -B -X utf8 tools/validate_plan.py` | exit 0, 96개 상세 task·오류 0 |
| Windows `python -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` | 94 tests, 5.559초, OK, skip 0 |
| WSL Ubuntu-26.04 `python3`의 `sys.version` | Python 3.14.4, GCC 15.2.0 |
| WSL 같은 SPDX·link·plan 명령 | 각각 exit 0, SPDX 13/오류 0, 문서 215/target 1836/오류 0, task 96/오류 0 |
| WSL `python3 -B -X utf8 -m unittest discover -s tests -p 'test_*.py'` | 94 tests, 4.817초, OK, skip 0 |
| `git diff --check b36c99fb6c7f4df2a27364842ee197fc947e461e HEAD` | exit 0, 공백 오류 없음 |
| `LICENSES/sources.json`의 file별 SHA-256을 `hashlib.sha256(Path(...).read_bytes())`로 대조 | 21개 사본, 불일치 0 |
| `gh pr view 2 --json number,url,isDraft,headRefOid,statusCheckRollup` | [PR #2](https://github.com/digitie/kor-travel-common/pull/2) draft, head가 candidate와 일치. [CI run 34061442672](https://github.com/digitie/kor-travel-common/actions/runs/34061442672)의 validate-docs SUCCESS |

실행은 모두 후보 detached checkout 기준이며 Python 임시 fixture는 `tempfile.TemporaryDirectory` 안에서 만들고 종료 때 정리했다. 추가 공격 CLI 사례는 Windows 12개·WSL 10개다. 아래 오류는 기존 94 tests에 포함되지 않아 정상 회귀 성공만으로 발견되지 않는다.

문서 target 수는 작성자의 작업 중 evidence 1833개와 달리 immutable candidate에서 양 OS 모두 1836개였다. 이 보고서는 실제 실행값을 사용한다. 문서 검사 변경은 LICENSES Markdown까지 범위를 넓힌 것이며 정상 회귀에서 기존 도구의 새 실패는 없었다. 나머지 기존 Python/CI 변경은 헤더 보완·Origin 표기 정리다.

## 공통 공격 fixture

다음은 실제 실행한 fixture의 최소 형태다. `PROVENANCE.md`와 `tools/sample.py`만 있는 임시 디렉터리를 만들고 candidate의 CLI를 절대 경로로 호출했다. 각 사례는 따로 생성했다. SHA는 재현용 고정 문자열이며 실제 소비자 코드를 반입하지 않는다.

```text
PROVENANCE.md:
| PV-001 | `tools/sample.py` | canview | `1234567890abcdef1234567890abcdef12345678` | `src/sample.py` | GPL-3.0-or-later | 변경 | 확인 |

tools/sample.py:
# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: 2026 작성자
print(1)

실행:
python -B -X utf8 <candidate>/tools/check_spdx.py --root <fixture>
```

이 기본형은 Origin과 Modified가 없으므로 Windows·WSL 모두 exit 1, 오류 2개를 정확히 반환한다. 아래 변형만 가하면 판정이 달라진다.

## A-T003-P1-01 — 경로 별칭으로 출처·수정 고지 검사가 사라짐

- 위치: [check_spdx.py](../../../../tools/check_spdx.py) 104~118행의 경로 등록, 141행의 실제 파일 조회.
- 근거: licensing §5.2와 T-003은 색인의 이식 소스에 Origin·수정 시 Modified를 요구한다. 그러나 입력 경로는 파일시스템에서 존재 여부를 확인하면서 dict key에는 원문 `name`을 넣고, 조회에는 정규화된 상대 경로를 사용한다.
- 재현: 위 공통 fixture의 파일 열만 `./tools/sample.py` 또는 `tools//sample.py`로 바꾼다. 실제 파일은 그대로 있고 Origin·Modified도 없다. Windows·WSL 모두 **exit 0, 1개 파일·오류 0**이다. Windows에서는 `TOOLS/sample.py`도 같은 오성공이며 Linux에서는 파일 없음 exit 1이다.
- 영향: 일반적인 상대 경로 표기 차이만으로 등록된 이식 파일을 자체 작성처럼 판정해 필수 고지 누락을 통과시킨다. OS에 따라 같은 색인의 판정도 달라진다. 같은 실제 파일의 중복 표기 역시 raw key 비교로는 안전하게 판정할 수 없다.
- 권고: source enumeration과 색인에 동일한 정규 경로 식별자를 사용하거나 비정규 표기를 명시적 입력 오류로 거부한다. `./`·중복 구분자·Windows 대소문자 별칭을 포괄하고 중복 등록도 같은 식별자로 검사한다. 잘못된 표기를 허용하더라도 등록 행을 조용히 잃으면 안 된다.
- Disposition: **OPEN**. 기존 `test_port_missing_origin_fails`는 정규 경로만 사용하므로 이 경계를 고정하지 못한다.

## A-T003-P1-02 — owner/repo 형식의 geo 원천이 -only 검사를 우회

- 위치: [check_spdx.py](../../../../tools/check_spdx.py) 19~20행의 Origin 저장소 문법, 117~118행의 색인 정보, 158~160행의 geo 판정.
- 근거: LIC-5·LIC-18·§5.2는 geo 원천에서 GPL-3.0-or-later 단독 표기를 실패로 요구한다. Origin 문법은 `digitie/kor-travel-geo`도 허용하지만 판정은 bare 이름 `kor-travel-geo`와만 비교한다. 색인의 원천 라이선스는 `파생` 여부 외에는 판정에 보존되지 않는다.
- 재현: 표의 repo를 `digitie/kor-travel-geo`, 원천 라이선스를 `GPL-3.0-only`, 수정 열을 `없음`으로 두고 헤더에 다음 Origin을 추가한다. SPDX는 GPL-3.0-or-later 그대로다.

```text
# Origin: digitie/kor-travel-geo@1234567890abcdef1234567890abcdef12345678 src/sample.py
```

- 실제: Windows·WSL 모두 **exit 0, 오류 0**. bare `kor-travel-geo`로 바꾸거나 선택 괄호 `(GPL-3.0-only)`를 추가한 대조군은 exit 1이다. Windows에서 다른 repo 이름과 색인의 `GPL-3.0-only`만 있는 경우도 exit 0이었다.
- 영향: 검사기가 허용하는 저장소 표기 차이로 명시된 원천의 제한을 잃은 헤더를 승인한다. 실제 배포의 법적 위반을 단정하는 것이 아니라 현재 라이선스 gate의 판정 오류다.
- 권고: 허용하는 owner/repo 표기에서 저장소 동일성을 일관되게 판정하고, 명시된 원천 라이선스와 파일 헤더의 계약도 대조한다. 선택적 Origin 괄호의 존재 여부만으로 필수 조건이 켜지면 안 된다. bare/qualified geo의 잘못된 헤더 실패와 올바른 -only/AND 헤더 성공을 모두 시험한다.
- Disposition: **OPEN**. 기존 geo 회귀는 bare 이름만 시험한다.

## A-T003-P2-03 — 출처 표의 선행 공백을 조용히 무시

- 위치: [check_spdx.py](../../../../tools/check_spdx.py) 91~94행.
- 근거: `line.strip()`은 cell 분리 단계에서만 실행되고 PV 행 선택 regex는 첫 글자가 `|`일 때만 작동한다. 출처 표가 공백으로 들여써지면 검증할 행 자체가 없는 것으로 처리한다. 정본에는 이 표기를 조용히 무시한다는 계약이 없다.
- 재현: 공통 fixture의 PV 행 앞에 공백 1개 또는 3개를 넣는다. Windows에서 둘 다 **exit 0, 오류 0**이며 WSL에서도 공백 1개 사례가 같은 결과다. 실제 Source에 올바른 Origin·Modified를 모두 넣은 대조군은 반대로 `Origin에 대응하는 PROVENANCE 소스 행 없음`으로 exit 1이다.
- 영향: 같은 표의 공백 편집이 잘못된 소스에는 오성공, 올바른 소스에는 오실패를 만든다. 파일 내용이나 실제 출처는 바뀌지 않았는데 검사 결과가 뒤집힌다.
- 권고: 지원하는 table 행의 선행 공백을 일관되게 처리한다. 제한 문법을 선택한다면 PV 행처럼 보이는 잘못된 입력을 명시적으로 실패시키고 빈 색인처럼 통과시키지 않는다. 정상 헤더/누락 헤더 양쪽 회귀를 추가한다.
- Disposition: **OPEN**.

## 추가 경계에서 정상 동작한 부분

WSL에서 실제 소스 심볼릭 링크는 exit 1, 디렉터리 심볼릭 링크도 exit 1이었다. 임시 파일의 읽기 권한을 제거한 사례는 Permission denied를 잡아 exit 1로 반환했고 traceback 없이 종료했다. 읽기 권한은 정리 전에 복원했다. 기존 회귀 26개는 헤더 부재·빈 값·문자열/docstring 위장·주석 문법·미완료 블록·shebang·Origin 불일치·Modified 날짜·빈 scope·잘못된 인코딩 등을 검사하며 양 OS 전체 시험 안에서 실행됐다.

원천의 실제 코드 복사·금지 원천 여부를 자동 추론하지 않는 한계는 licensing §5.2와 PROVENANCE가 명시하므로, 그런 자동 탐지 기능의 부재 자체를 finding으로 만들지 않았다. 발견한 세 건은 이미 제공된 색인/헤더를 대조하는 약속 안의 실패다.

## 미검증과 최종 판정

- Python 3.11 런타임 직접 실행: **NOT_RUN**. Windows `py -0p`는 3.14·3.10만 보였으며 정본 floor인 3.11 실행기를 확보하지 않았다. 실제 검증 OS/Python은 위 표와 같다.
- Windows 네이티브 심볼릭 링크·ACL 거부: **NOT_RUN**. 해당 실제 파일 경계 검사는 WSL에서 수행했다.
- 원격 라이선스 원문·npm tarball 재다운로드/재대조, 소비자 원본 Git object의 독립 대조: **NOT_RUN**. 로컬 digest 21개 일치는 기록된 사본의 무결성만 뜻한다.
- SPDX 필수 CI 단계·Windows matrix: **NOT_RUN**. T-009 후속 gate이며 현재 validate-docs SUCCESS를 그 성공으로 세지 않는다.
- 패키지 build·tarball/wheel 설치·소비자 빌드/e2e·실제 배포: **NOT_RUN(제품 실물 없음·소비자 변경 금지)**.

**BLOCK**. 정상 94 tests와 현재 트리 검사 성공이 두 P1 및 P2의 재현 결과를 해소하지 않는다. 수정 commit에서 위 표기 경계와 전체 delta를 두 reviewer가 다시 검토해야 한다.
