# T-005a `uv.lock` 파서 적대적 리뷰 통합 판정

- task: [T-005a](../../tasks/T-005a-check-versions-uv-lock.md)
- 최초 candidate: `3bc0f473833aa51a8656b045b705891debf8cf13`
- base: `6e1881b86f017d604ccfa416bd368e5aba24c669`
- 최종 candidate: `3c5801f14855a067080f257ec83d2279de32c74a`
- 최종 판정: **A PASS / B PASS**, 누적 finding 8개 모두 FIXED.

## 독립 실행

최초 A/B는 같은 manifest와 최초 candidate를 각각 새 detached worktree에서 검토했고 상대 결과를 보지 않았다. 이후 수정 기준마다 A/B가 독립 post-fix 검토를 반복했다. 원본은 [evidence 디렉터리](evidence/2026-09-07-t005a-manifest.md)에 보존했다.

| 단계 | candidate | A | B | 핵심 결과 |
|---|---|---|---|---|
| 최초 | `3bc0f47` | BLOCK | BLOCK | 고정 선언 우회, 구조 fail-open, PEP 735 오류 은닉, 오류 원문 노출 |
| post-fix 1 | `9027500` | BLOCK | BLOCK | URL/group 경계·순환·정규화·`branch` 내부 `@` 잔여 |
| post-fix 2 | `735efed` | PASS | BLOCK | `rev/tag=topic@v1.2.3`가 마지막 조각만 태그로 해석되는 잔여 |
| post-fix 3 | `3c5801f` | **PASS** | **PASS** | 누적 반례와 신규 경계 모두 차단, 신규 finding 0 |

각 단계의 manifest·실행 ID·SHA·clean 상태·명령·미실행 범위는 [reviewer evidence](evidence/2026-09-07-t005a-post3-reviewer-a.md), [reviewer evidence](evidence/2026-09-07-t005a-post3-reviewer-b.md)와 post-fix evidence에 연결했다.

## Finding disposition

| ID | 심각도 | disposition | 반영 기준 |
|---|---:|---|---|
| A-P1-01 / B-P1-01 | P1 | **FIXED** | `9027500`: 직접 고정 선언도 lock의 모든 git source와 marker 순서를 독립 대조 |
| A-P1-02 | P1 | **FIXED** | `735efed`: URL hostname/port·lock dependency/group 내부 자료형 fail-close |
| A-P2-03 | P2 | **FIXED** | `735efed`: PEP 735 정규화·중복·순환·정확한 include key·누락 group 검사 |
| A-P2-04 | P2 | **FIXED** | `9027500`: uv/manifest 입력 오류의 원문 이름·값·경로 비공개 |
| B-P1-02 | P1 | **FIXED** | `735efed`: 이름 모양과 무관한 branch를 FLOATING_REF로 판정 |
| A-P2-05 | P2 | **FIXED** | `735efed`: `Test_Group`/`test-group` 정규화와 충돌 거부 |
| B-P1-03 | P1 | **FIXED** | `3c5801f`: decoded path의 `@` 중복을 보수적으로 부동 처리해 rev/tag 우회 차단 |

초기 A/B가 같은 근본 원인을 각각 보고한 항목은 원 ID를 유지하고 disposition을 함께 기록했다. post-fix A/B의 구체 실행은 [A 원본](evidence/2026-09-07-t005a-post3-reviewer-a.md), [B 원본](evidence/2026-09-07-t005a-post3-reviewer-b.md), [post2 B 원본](evidence/2026-09-07-t005a-post2-reviewer-b.md)에 있다.

## 최종 검증

- Windows Python 3.14.3 전체 **140 tests·skip 0**, WSL Python 3.11.15 전체 **140 tests·skip 0**; `test_check_versions.py` **45 tests**.
- 최종 A: 양 OS 전체 회귀, 독립 CLI 55사례, 공유 lock 3사례, link 272/2151, plan 102, SPDX 20, secret/redaction 335파일, self-check를 실행해 오류 0.
- 최종 B: 양 OS 전체 회귀, 독립 CLI 32사례, URL·encoding·SSH authority 경계, link 272/2151, plan 102, SPDX 20, self-check를 실행해 오류 0.
- PR #6 run `34080403871`의 `docs`, `tools (ubuntu-24.04)`, `tools (windows-2025)`, `secret-scan`, `check-versions`가 모두 success다.
- 실제 공항 저장소는 파일을 쓰지 않고 양 OS에서 `findings=24 failing=2 exit=0`을 재현했다. 이는 소비자 제품 gate 성공이 아닌 정적 관찰이다. weather fixture는 `findings=2 failing=0 exit=0`이다.

## 한계

실제 소비자 `uv sync --locked`·설치·빌드·e2e, 모든 upstream lock 생성 형식의 동등성, npm/PyPI 게시·Release·태그·다른 저장소 쓰기는 **NOT_RUN**이다. `uv.lock`은 제한된 schema/source 경계만 읽으며 실제 resolver·설치 성공을 대신하지 않는다.
