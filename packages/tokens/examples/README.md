# 토큰 예제

`weather-overrides.css`는 `tokens.css`와 `aliases/map-vocabulary.css`를 적용한 뒤
weather의 현재 navy 브랜드, 17rem rail, 폰트 스택을 재현하는 앱 소유 오버라이드 예제다.
이 디렉터리는 npm 배포 대상이 아니다. 실제 weather 교체와 320·375·414·768·1024·1280px
6폭 시각 diff 0 검증은 T-461의 소비자 저장소 evidence에서 수행한다(`NOT_RUN(T-461)`).
