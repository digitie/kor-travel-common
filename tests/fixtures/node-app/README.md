# Node workflow fixture

`versions-check`가 React 18.3.1과 Node 22.12.0의 의도적인 하한 미달을
report 모드로 기록하는 최소 fixture다. `contrast-overrides.css`는 대비
미달 입력을 만들며 report 기본에서 workflow를 실패시키지 않는다.

문서 링크 검증 대상은 [package manifest](package.json)다.
